# CLAUDE.md — Polymarket Up/Down Trading Bot (Rust)

This file is the single source of truth for this project. Read it fully before every task. If anything you discover in the official docs contradicts this file, the docs win — then update this file and note the change in the Decisions Log at the bottom.

---

## 1. What we are building

A high-performance Rust trading bot for Polymarket's short-duration crypto Up/Down markets, with:

- **Universe (initial):** BTC and ETH × 5-minute, 15-minute, and 1-hour Up/Down series (6 series total). The dashboard's per-series comparison will later be used by the operator to narrow down to the best-performing 2–3 series.
- **Two modes, one engine:** Paper and Live. Paper mode consumes **real live Polymarket market data** — real books, real trades, real resolutions, real fee math — only the money is simulated, with operator-adjustable starting capital. The strategy engine must be 100% identical between modes; only the execution venue adapter differs.
- **24/7 autonomy:** when a window resolves, the bot automatically rolls to the next window of that series, forever, with no human action.
- **Strategy (summary, full spec in §8):** model-based two-sided quoting (market making) around a fair value computed from the underlying price feeds, with cancel-first repricing, inventory-skewed quotes, strict pair-cost discipline, and a fee-aware taker module for momentum/late-window opportunities. The core failure mode we are engineering against is adverse selection: resting quotes getting picked off by faster traders when the underlying moves.
- **Dashboard:** served by the bot itself, showing both paper and live sessions, per-series performance comparison, live window view, fills, risk state, and controls (including paper capital adjustment).
- **Deployment target:** Linux VPS located close to Polymarket's infrastructure (operator will choose region using our latency benchmark tool). Development happens on the operator's PC — build performance rules in §3 are mandatory.

The human operator writes no code. You (Claude Code) implement everything. The operator gives you one focused prompt at a time from BUILD_PLAN.md.

---

## 2. Hard rules for every session

1. **Read before you write.** Before implementing anything that touches a Polymarket API, fetch and read the relevant docs page (map in §13). The complete docs index lives at https://docs.polymarket.com/llms.txt — use it to discover pages. Never invent endpoint paths, field names, message shapes, or auth flows. If a field is not confirmed by docs or by a live API response you inspected, do not use it.
2. **One task, fully done.** Each prompt covers one module/function. Finish it to Definition of Done (§12) before touching anything else. Do not "improve" unrelated code in passing.
3. **Never break the workspace.** Every task ends with the whole workspace compiling, lints clean, and all existing tests passing — not just the crate you touched.
4. **Paper-first, live-gated.** The bot must be incapable of sending a live order unless live mode is explicitly armed (config flag AND environment confirmation AND dashboard arm action, per §11). Default everything to paper. Treat this as a safety invariant, not a feature.
5. **Identical engine paths.** Strategy/engine code must never know which venue it is talking to. If you find yourself writing "if paper / if live" inside engine logic, stop and refactor — mode differences live only in the venue adapters.
6. **Money is decimal, never float.** All prices, sizes, fees, balances use a decimal type (rust_decimal). Floating point is allowed only inside the math model (vol, normal CDF, fair value), and converted at the boundary with explicit rounding rules.
7. **Verify against live reality.** Where the docs are ambiguous (exact market slugs, strike/price-to-beat source, field presence), write a small read-only probe against the live public API, inspect actual responses, then implement and record what you learned in the Decisions Log.
8. **Update this file.** When you make an architectural decision, learn an API fact, or change a parameter default, append it to the Decisions Log (§15) in the same task.

---

## 3. Build performance (mandatory — the operator's previous Rust project made his PC unusable)

You must configure the project so that day-to-day iteration is fast and the machine stays responsive. Implement all of the following at scaffold time and never regress them:

- **Many small workspace crates** (see §4). Rust recompiles at crate granularity; small crates = small rebuilds. Engine logic must not live in one giant crate.
- **Dev profile:** workspace members compile unoptimized; **all external dependencies compile at optimization level 2 via a profile package override** so they are compiled once, cached, and fast at runtime even in dev. Reduce debug info to line-tables-only in dev to shrink link times.
- **Fast linker:** configure the mold linker on Linux (fallback: lld) through the cargo configuration. Document the one-time system install step in the README.
- **Cap parallelism:** set the default build jobs to (logical cores − 2) via cargo configuration so builds never starve the operator's desktop. Document how to override for the VPS.
- **Iteration workflow:** developers (you) iterate with cargo check / clippy via a watcher (bacon), not full builds. Full release builds happen only for deployment. Encode these as just recipes.
- **Dependency discipline:** TLS is rustls everywhere — never openssl. Keep the dependency tree minimal. Allowlist (add others only with written justification in the Decisions Log): tokio, tokio-tungstenite, serde + serde_json, reqwest (rustls feature), rust_decimal, thiserror, anyhow, tracing + tracing-subscriber, axum + tower, rusqlite or sqlx-sqlite, figment or config, chrono or time, the official Polymarket Rust SDK and only what it transitively requires, flate2 (pure-Rust `rust_backend`, journal segment gzip — operator-mandated, Decisions Log 2026-06-14). Forbidden in the bot workspace: polars, full ethers, openssl-sys, any ORM, any heavyweight GUI toolkit.
- **Feature-gate heavy optional parts** (e.g., replay analytics) so the core bot builds lean.
- A fresh contributor following the README must get a working check loop in under 10 minutes.

---

## 4. Workspace architecture

Cargo workspace, one binary, small library crates. Names are suggestions — keep them short and consistent.

| Crate | Responsibility | Depends on |
|---|---|---|
| core-types | Domain types, internal event enums, ids, decimal newtypes, rounding helpers | — |
| config | Layered config (file defaults + env overrides), typed per-subsystem structs, secret handling | core-types |
| timeutil | Monotonic/wall-clock discipline, NTP offset check, τ-remaining helpers, latency harness | core-types |
| discovery | Gamma/CLOB REST market discovery for the 6 series; market metadata cache | core-types, config |
| scheduler | Per-series 24/7 window lifecycle state machine and rollover | core-types, discovery, config |
| feed-util | Shared feed plumbing: WS transport seam, generic reconnect/staleness driver, backoff, scripted fake transport (feature `fake`) | core-types |
| feed-rtds | Polymarket RTDS WebSocket (Chainlink + Binance price topics) | core-types, feed-util |
| feed-binance | Direct Binance WebSocket (lowest-latency signal) + feed-comparison diagnostics | core-types, feed-util |
| feed-clob | Polymarket market-channel WebSocket + local L2 book maintenance | core-types, feed-util |
| model | Vol estimator, fair-value engine, basis tracker, health states | core-types |
| venue-api | The execution port (trait): place/cancel/cancel-all/balances/event-stream | core-types |
| venue-live | Live adapter over the official Polymarket Rust SDK; user-channel WebSocket; order store reconcile | venue-api |
| venue-paper | Paper matching engine, paper ledger/wallet, fee+rebate simulation | venue-api, feed-clob types |
| engine | Inventory/pair accounting, quoting calculator, quote manager, taker modules, risk manager | model, venue-api |
| journal | Append-only event log (sqlite + raw tick files), replay reader | core-types |
| analytics | Markouts, PnL attribution, per-series aggregates | journal |
| dashboard | Axum REST + WebSocket push + static UI | analytics, engine state |
| bot (binary) | Orchestrator: wires everything, supervision, control plane, CLI subcommands | all |

Rules: dependency arrows point inward only (no crate may depend on `bot`; `engine` must not depend on `venue-live` or `venue-paper`, only `venue-api`). Communication between long-running tasks goes through typed channels carrying core-types events — no shared mutable state across async tasks except small lock-protected snapshots for the dashboard. In hot paths (tick → model → quote decision): no heap allocation where avoidable, no locks held across await points, no synchronous I/O.

---

## 5. Runtime topology (one process)

Long-running tasks: rtds feed, binance feed, one clob feed per active window pair, scheduler, model (per asset), engine (per series), venue adapter, journal writer, analytics ticker, dashboard server, control plane. The scheduler announces window lifecycle events; engines attach to windows; the risk manager sits between every engine and the venue — **no order reaches a venue without passing risk checks**. Graceful shutdown order: stop strategies → cancel all open orders → flush journal → exit.

---

## 6. Trading universe & 24/7 rollover

- Series: BTC-5m, BTC-15m, BTC-1h, ETH-5m, ETH-15m, ETH-1h. Each series is independently enable/disable-able at runtime (dashboard + config). All six run concurrently in paper for the evaluation phase.
- Windows are pre-created by Polymarket on a fixed schedule. Discovery must always hold: current active window + at least the next window per series, fetched ≥60 seconds before the current one ends.
- The market WebSocket (with the custom feature flag enabled, §7) emits `new_market` and `market_resolved` events — use these as the primary rollover triggers, with the wall clock + discovery refresh as the fallback so a missed event can never stall a series.
- Per-series lifecycle: Discovering → Pending (subscribed, waiting for open) → Active (trading) → Closing (final-seconds behavior per §8) → Resolved (settle, merge/redeem, attribute PnL) → roll to next. A series with no discoverable next window logs loudly, retries with backoff, and never crashes the process.
- Resolution rule for these markets: outcome is Up if the resolution-source end price is **greater than or equal to** the start price ("price to beat"). Ties resolve Up. Resolution source for the 5m/15m crypto series is the **Chainlink BTC/USD (resp. ETH/USD) data stream** — confirm the 1h series' rules text via the API at discovery time and store the resolution source per market rather than assuming.

---

## 7. Polymarket API facts (verified June 2026 — re-verify via docs when implementing)

**Hosts**
- CLOB REST: https://clob.polymarket.com
- Discovery (Gamma): https://gamma-api.polymarket.com — see docs page "Fetching Markets" for current endpoints/params
- Market WebSocket: wss://ws-subscriptions-clob.polymarket.com/ws/market
- User WebSocket (authenticated): wss://ws-subscriptions-clob.polymarket.com/ws/user
- Real-Time Data Socket (RTDS): wss://ws-live-data.polymarket.com
- Docs index for discovery: https://docs.polymarket.com/llms.txt

**Official Rust SDK exists — use it.** The docs show first-class Rust examples under the name polymarket-client-sdk-v2: order builders (limit and market), separate sign and post steps, batch posting, automatic fetch of tick size / neg-risk / fee rate inside the builder. Locate the crate via the docs Client Reference pages and crates.io; pin the version. Do not hand-roll EIP-712 signing or auth headers unless the SDK is missing something — if it is, document why in the Decisions Log.

**Auth & wallets.** New API users are directed to **deposit wallets** (see docs "Deposit Wallets"; signature types include EOA = 0, proxy = 1, Gnosis Safe = 2, deposit wallet = 3). Live trading also requires one-time token approvals for the exchange contracts — follow the docs "Trading Quickstart" + "Deposit Wallets" exactly. Collateral/balance denomination: read docs "Polymarket USD (PUSD)" concept page during wallet/ledger work and follow it.

**Order types & behaviors**
- GTC, GTD (limit, rest on book) and FOK, FAK (marketable, execute immediately).
- **Post-only** flag exists for GTC/GTD: the order is rejected instead of matching if it would cross. Our maker quotes should use post-only so we never accidentally pay taker fees.
- GTD has a 60-second security threshold: for an effective lifetime of N seconds, set expiration = now + 60 + N.
- Marketable orders: BUY is specified as a dollar amount, SELL as a share count; the price field on them is a worst-price slippage limit, not a target.
- Batch endpoint: up to 15 orders per request.
- Cancels: by order id, by market (cancel-market), and cancel-all. Read docs "Cancel Orders" for exact endpoints. Wire cancel-all into every safety path.

**Market channel (public)** — subscribe with a JSON message containing the list of token ids under assets_ids, type set to "market", and custom_feature_enabled set to true (this unlocks best_bid_ask, new_market, and market_resolved events). Client must send the text `PING` at least every 10 s (we send every 5 s); the server replies `PONG`. A text frame carries either ONE event object or a JSON ARRAY of them (arrays observed live on the multi-token subscribe delivery). Event types, each tagged by an event_type field:
- book — full snapshot on subscribe and whenever a trade affects the book; arrays of {price, size} for bids and asks; includes asset_id, market (condition id), timestamp, hash (opaque — algorithm undocumented). **Trades reach the books ONLY through these snapshots** (~1.2/s per token observed on an active BTC-5m pair); price_change never carries trade effects.
- price_change — order placed/cancelled; carries a list of per-asset changes each with price, size (the new TOTAL at that level), side, and current best_bid / best_ask; a size of "0" means that level was removed. One order appears MIRRORED as two entries (BUY Up at p ↔ SELL Down at 1−p, same size). **A trade at the touch arrives as a crossing change** (a bid AT the best ask) carrying the post-trade tops — the consumed opposite level's removal is never sent as a delta; resolve locally by implied consumption (drop opposite levels at-or-better than the new price), confirmed by the next snapshot (verified live 2026-06-12; naive crossed-book-recycle looped on every such trade).
- tick_size_change — fired when price crosses 0.96 / 0.04 and tick flips between 0.01 and 0.001. The book and our quoting must handle this live. (Not yet captured live — fixture synthesized from docs.)
- last_trade_price — trade prints (used by the paper fill simulator and markouts); can arrive after the trade's book snapshot (ordering not guaranteed).
- best_bid_ask, new_market, market_resolved — via the custom feature flag. new_market is a PLATFORM-WIDE broadcast (every new market on the venue, any category — dedup + treat as refresh hint only). market_resolved arrives on connections subscribed to that market's tokens; its condition id is in `market` (no condition_id/slug field despite the docs' field list), winner in `winning_asset_id`. A closed window's connection must linger to receive it (~20–60 s after close observed).
- Load shape: one active 5m token pair ≈ up to ~800 msg/s peak; 12 tokens on one connection floods ~1500 msg/s and the server RSTs it — one connection per window pair (§5).

**User channel (authenticated)** — same host, /ws/user path; API credentials go inside the subscription message; streams your order updates and trades filtered by API key. Read the full docs page for exact auth fields and event schemas before implementing; reconcile with REST open-orders on every reconnect.

**RTDS (no auth, critical for us)** — send PING every 5 seconds; subscriptions are JSON messages with action subscribe/unsubscribe and a list of {topic, type, filters}; messages arrive as {topic, type, timestamp, payload}.
- Topic crypto_prices = **Binance source**; symbols lowercase concatenated (btcusdt, ethusdt). Topic crypto_prices_chainlink = **Chainlink source — this is the resolution feed for these markets**; symbols slash format (btc/usd, eth/usd); subscribes with type "*".
- **One filter slot per (connection, topic), replace-not-add** (verified live 2026-06-12; regression probes in `crates/feed-rtds/tests/live_probe.rs`): a later filtered subscribe on a topic replaces the earlier filter, and no multi-symbol filter form works (comma lists and JSON arrays are acked then silent — including the docs' plain comma-list form). Multi-symbol streaming = the all-symbols form (crypto_prices: omit `filters`; chainlink: `"filters":""` with type "*"), ~1 update/s per symbol (~13 msg/s for everything), filtered client-side.
- A **filtered** subscribe (JSON-encoded-string filter `"{\"symbol\":\"btcusdt\"}"` — the only filter form the server honors) triggers a ~2-minute **backfill** message (`payload {symbol, data:[{timestamp, value}…]}`, type "subscribe"); the all-symbols form does not. Chainlink backfills arrive under the WRONG topic (crypto_prices) — stream identity must be resolved from the symbol, never the topic.
- Live updates additionally carry `full_accuracy_value` (lossless string: Binance plain decimal, Chainlink 18-dp fixed-point integer = value×10¹⁸) and `connection_id`. The subscribe ack is an empty text frame; no text "PONG" reply was observed from RTDS. The server can answer a subscribe with an error frame `{body:{message}, statusCode:500}` — observed live, left one symbol's stream dead for an entire session; feed-rtds self-heals by recycling the connection when a tracked stream starves.
- Subscriptions can be modified without reconnecting.
- Note: Chainlink offers a sponsored direct Data Streams API key for 15-minute-market traders via a request form linked on the RTDS docs page — worth the operator applying later; design feed-rtds so a direct Chainlink source could be slotted in.

**Fees (exact, from docs "Fees")**
- Formula: fee = shares × feeRate × p × (1 − p), charged to **takers only**, in dollars, at match time by the protocol (nothing to include in orders). Makers pay zero.
- **Crypto category feeRate = 0.07, maker rebate share = 20%.** Reference points per 100 shares: $1.75 at p=0.50, $1.31 at 0.25/0.75, $0.63 at 0.10/0.90, $0.07 at 0.01/0.99. Symmetric around 0.50.
- Fees rounded to 5 decimals; minimum charged fee 0.00001.
- Per-market fee params are queryable (the SDK exposes a market-info call returning a fee descriptor with rate/exponent/taker-only fields) and feesEnabled is on the market object — **always read fee params per market at discovery; never hardcode 0.07** in logic (hardcode only as a config default).
- Maker Rebates Program: 20% (crypto) of collected taker fees redistributed **daily in USDC**, proportional to your executed maker volume, $1 minimum accrual — read docs "Maker Rebates Program" for the precise accrual definition when implementing the rebate estimator. A separate tiered **Taker Rebate Program** exists (docs "Taker Rebates") — read and incorporate into taker edge math if material. A separate **Liquidity Rewards** program also exists for resting two-sided quotes — optional income, read its page during the quoting-engine task.

**Order size & price constraints (operator-confirmed + enforce defensively)**
- Minimum resting order: **5 shares AND $1 notional** — i.e., minimum shares = max(5, ceil_to_size_precision(1.00 / price)), then re-validate after tick rounding. At 0.10, $1 binds (10 shares); at 0.50, 5 shares bind ($2.50). Also read minimum_order_size and minimum_tick_size from the market object and take the strictest constraint. Every outgoing order passes the normalizer (§ engine) — a rejected-for-size order in production is a bug.
- Tick sizes possible: 0.1 / 0.01 / 0.001 / 0.0001; our markets are 0.01 flipping to 0.001 past 0.96/0.04. Prices must always land on the current tick grid; valid range is [tick, 1 − tick].

**Other pages that must be read when relevant:** "Matching Engine Restarts" (handle restarts: assume orders may be gone, reconcile + re-quote), "CTF: Merge" and "CTF: Redeem" (matched Up+Down pairs merge back to $1 of collateral — our capital-recycling mechanic; redemption after resolution), "Gasless Transactions" (relayer for those on-chain ops), "Resolution", "Order Lifecycle", "Resources → Contracts" and "Resources → Error Codes" (map API error codes to typed errors), "Builders" (optional order attribution).

---

## 8. Strategy specification (the engine's brain)

**Fair value.** For a window with strike K (the "price to beat" captured at window start from the resolution-grade feed), current price S, and τ seconds remaining: z = ln(S/K) / (σ_1s × √τ), and p_up = Φ(z) (standard normal CDF; drift is negligible at these horizons; apply the ≥-ties-go-Up rule as a tiny adjustment only if measurement shows it matters). σ_1s is an EWMA of 1-second log returns (configurable half-life, default 60s) with a floor and cap. Chainlink (via RTDS) is ground truth; direct Binance is the fast signal, corrected by a continuously-estimated Chainlink-minus-Binance basis. The model exposes a health state; when inputs are stale/diverging it declares UNRELIABLE and the engine pulls quotes.

**Quoting.** Desired quotes are computed as a pure function of (model state, inventory, fee params, config, τ, tick): center = p_up − γ × normalized inventory imbalance; half-spread = max(configured minimum edge, k1 × σ_1s × √(expected holding time) + a latency premium derived from our measured cancel round-trip). Two-sided, post-only, small size at the touch with a ladder behind. Gates: no quoting while model UNRELIABLE; widen on vol spikes; **no at-the-money quoting in the final 20–30s; no passive orders at all in the final ~5s (cancel-all)**; respect per-window inventory caps.

**Cancel-first repricing.** On any signal tick that moves p_up against a resting quote beyond a threshold θ, cancel first (cancel-market on large moves), then re-quote. This single behavior is the primary defense against the adverse-selection failure mode (resting quotes being picked off by faster traders the instant the underlying moves) — treat its latency as a first-class metric.

**Inventory & pair discipline.** Track per window: shares and cost per side, matched pairs = min(Q_up, Q_down), pair cost = avg_up + avg_down, signed excess, and worst-case-if-excess-loses dollars. Only add to a side passively if the post-trade pair cost stays ≤ the configured threshold (default 0.98). Soft cap on excess → quote only the deficit-reducing side; hard cap → stop making, optionally work excess off at/above fair. Merge matched pairs back to collateral (live: CTF merge via relayer; paper: simulated instantly-credited merge) to recycle capital across windows.

**Taker modules (fee-aware).** (a) Momentum/staleness: when a confirmed signal-feed move implies the Polymarket book is stale, take with FAK only if model-vs-book edge exceeds fee(p) + configured buffer — at p≈0.5 the crypto fee is ≈1.75¢/share, so mid-window takes need real edge. (b) Late-window: when τ is below a threshold and p_up is beyond a certainty threshold on the Chainlink-anchored price, buy the near-certain side up to a price cap, sized against displayed depth (fees are near zero out there). Per-window taker budgets and cooldowns apply. Risk manager can veto everything.

**Per-window risk bound.** The configured maximum worst-case loss per window is the binding constraint on all sizing.

---

## 9. Paper trading specification

- Same engine, same risk manager, same order normalizer; only the venue adapter differs.
- **Fill simulation (conservative by design):** a passive paper order is queued behind ALL displayed size at its level at placement time (pessimistic queue position); it fills only as real last_trade_price prints occur at or through its price, after a simulated placement latency; cancels take effect after a simulated cancel round-trip. Marketable paper orders walk the real displayed book after the simulated latency with partial fills per available depth. Latencies are configurable distributions (defaults: operator-measured RTTs from the latency harness). It must be impossible for paper to be more optimistic than reality in any code path — when in doubt, fill less.
- **Money:** paper wallet starting capital comes from config and is adjustable at runtime from the dashboard (changes are journaled). Taker fees are charged using the live per-market fee params and the exact formula. Maker rebates are accrued as an estimate (rebate share × fee formula applied to our filled maker volume) and credited on a simulated daily cycle, reported separately from trading PnL.
- **Settlement:** positions settle on the real market_resolved outcome. Simulated merge converts matched pairs to cash instantly when the engine requests it.
- Paper and live ledgers share one schema; every event is journaled identically so analytics and dashboard treat both modes uniformly.

---

## 10. Dashboard specification

Served by the bot (axum), token-protected, REST snapshots + WebSocket push, single-page static UI with minimal dependencies, usable on a phone. Views:

1. **Overview:** mode badges (paper running / live armed-or-off), equity curves per mode, paper capital editor, global kill button.
2. **Series comparison (the decision table):** one row per series (BTC/ETH × 5m/15m/1h) with windows traded, % of windows profitable, net PnL, PnL per window, locked-pair PnL vs inventory PnL split, fees paid, rebates earned, average 5-second markout on passive fills, fill counts, and a minimum-sample warning. Sortable. This is how the operator picks the best 2–3 series — make it excellent.
3. **Live window view:** per active window — book ladder with our quotes highlighted, model fair vs book mid, countdown, inventory, pair cost, recent prints.
4. **Fills blotter:** chronological fills with markout coloring and attribution tags.
5. **Risk panel:** every breaker's state, feed health, WS connectivity, last cancel-all reason.
6. **Controls:** enable/disable per series, safe-listed live-tunable parameters, paper capital, live arming flow (multi-step, §11).

---

## 11. Risk rules (hard-wired)

All order flow passes through the risk manager. Non-negotiable behaviors: cancel-all on market-WS or user-WS disconnect, on any feed staleness beyond 500 ms, on detected matching-engine restart, and on clock-skew alarm. Per-window maximum loss; daily stop-loss that halts all trading until manual reset; global maximum open notional; stand-down (pull quotes) whenever |model fair − book mid| exceeds a sanity bound for more than a few seconds; error-rate circuit breaker on API failures; manual kill from dashboard and CLI. **Live arming:** live orders are only possible when the config enables live mode AND a specific environment variable contains a required confirmation phrase AND the operator has armed live mode in the dashboard for the session; absent any one of these, the live adapter must refuse to construct.

---

## 12. Coding standards & Definition of Done

Stable Rust. rustfmt enforced; clippy with warnings denied. No unwrap/expect/panic on runtime paths — typed errors (thiserror) inside crates, anyhow only at the binary boundary. Structured logging via tracing with per-subsystem targets; every order intent, placement, cancel, fill, and breaker event logged with ids and monotonic + wall timestamps. Async: tokio; no blocking calls on the runtime; channel back-pressure handled explicitly.

A task is Done only when: (1) the whole workspace passes fmt, clippy-deny-warnings, and all tests; (2) the new module has unit tests plus, where applicable, fixture-driven integration tests (recorded real API messages as fixtures — capture them, commit them); (3) public items have doc comments; (4) README/justfile updated if commands changed; (5) the Decisions Log records anything learned; (6) you state explicitly how the module integrates with its neighbors and demonstrate it (a runnable subcommand, a test, or a wired path in the bot binary).

---

## 13. Docs map (read the relevant rows before implementing each area)

| Area | Pages |
|---|---|
| Orientation | /polymarket-101, /quickstart, /concepts/markets-events, /concepts/prices-orderbook, /concepts/positions-tokens, /concepts/order-lifecycle, /concepts/resolution, /concepts/pusd |
| Discovery | /market-data/overview, /market-data/fetching-markets |
| Market data WS | /market-data/websocket/overview, /market-data/websocket/market-channel, /market-data/websocket/user-channel, /market-data/websocket/rtds |
| Trading & SDK | /trading/overview, /trading/quickstart, /trading/deposit-wallets, /trading/orderbook, /trading/orders/overview, /trading/orders/create, /trading/orders/cancel, /trading/orders/attribution, /trading/clients/public, /trading/clients/l1, /trading/clients/l2, /trading/gasless, /trading/matching-engine |
| Fees & rebates | /trading/fees, /trading/taker-rebates, /market-makers/maker-rebates, /market-makers/liquidity-rewards |
| Market-maker ops | /market-makers/overview, /market-makers/getting-started, /market-makers/trading, /market-makers/inventory, /market-makers/combos |
| On-chain ops | /trading/ctf/overview, /trading/ctf/merge, /trading/ctf/redeem, /resources/contracts |
| Errors & misc | /resources/error-codes, /advanced/neg-risk (our markets are binary: neg_risk false — verify per market) |
| Index | /llms.txt |

(All relative to https://docs.polymarket.com)

---

## 14. Glossary

Window = one tradable interval (e.g., BTC Up/Down 11:55–12:00). Series = the repeating product (BTC-5m). Strike / price-to-beat = resolution-source price at window start. Pair cost = combined average cost of one Up + one Down share. Excess = unmatched shares on one side. Markout = signed fair-value move a short interval after a fill; persistently negative markouts on passive fills = we are being adversely selected. Edge = model fair minus executable price, after fees. Merge = converting matched pairs back into $1 collateral. Armed = live trading explicitly enabled per §11.

## 15. Decisions Log

(Claude Code: append dated entries here — API facts verified, deviations from this file, parameter defaults chosen, dependency additions with justification.)

- 2026-06-11 — Project initialized from this spec. Verified from docs: Rust SDK exists (polymarket-client-sdk-v2 naming in docs), RTDS carries both Binance and Chainlink crypto prices unauthenticated, crypto taker feeRate 0.07 with 20% maker rebate, market-channel custom_feature_enabled unlocks new_market/market_resolved, GTD +60s threshold, batch limit 15, post-only supported, tick flips to 0.001 beyond 0.96/0.04.
- 2026-06-11 — Workspace scaffolded: 16 library crates + `bot` binary exactly per §4, all empty compiling libs with module docs; zero external dependencies declared yet (allowlist enforced by omission). §3 implemented: `[profile.dev] debug = "line-tables-only"`, `[profile.dev.package."*"] opt-level = 2`, mold linker scoped to `x86_64-unknown-linux-gnu` in `.cargo/config.toml` (requires clang+mold; lld fallback documented in README), `jobs = 10` (operator's 12 logical cores − 2; VPS override via `CARGO_BUILD_JOBS`). Edition 2024, rust-version 1.94. Lints via `[workspace.lints]` + `[lints] workspace = true` in every crate: clippy all = warn, unwrap/expect/panic/todo/dbg = warn (denied by `just lint` via `-D warnings`; tests exempted in clippy.toml), `missing_docs` = warn, `unsafe_code` = deny. `clippy::print_stdout` allowed for now (banner) — revisit when tracing lands. Tooling: justfile (check/lint/fmt/test/watch/run-paper/build-release, windows-shell = powershell), bacon.toml, rustfmt.toml (style_edition 2024, max_width 100, Unix newlines), .gitignore. Measured on operator PC: from-scratch full check 0.65 s, no-op check 0.04 s, touch `model` → rebuilds only model/engine/dashboard/bot in 0.32 s. Note: crates named `config`/`model` shadow same-named crates.io packages; if the external `config` crate is ever chosen over figment, it must be renamed via `package =` rename in the dependency declaration.
- 2026-06-11 — `core-types` implemented (13 modules, 80 unit tests). First external deps added to `[workspace.dependencies]`, all allowlisted: rust_decimal 1.42 (features std/serde/macros — serde serializes Decimal as a lossless string, never serde-float; macros gives `dec!`), serde 1.0.228 (derive), thiserror 2.0.18; serde_json 1.0.150 declared workspace-wide but used **dev-only** in core-types for round-trip tests. Decisions: (1) timestamps are a self-contained `TimestampMs(i64)` unix-millis newtype + signed `DurationMs` — no chrono/time in the foundation crate (Polymarket wire timestamps are already ms; clock discipline lives in timeutil; keeps the 10-dependent-crate rebuild footprint minimal); core-types has no way to observe "now". (2) `Price` does NOT embed its TickSize (tick flips live via tick_size_change) — type invariant is strictly-inside-(0,1) ∧ scale ≤ 4, normalized; grid membership enforced by constructors (`on_grid` exact, `quantize` directional+clamping, `from_f64` model boundary) and MUST be re-checked by the engine normalizer against `MarketInfo.tick_size` before every placement. (3) `Price::from_f64` uses `Decimal::from_f64` (shortest round-trip), never `from_f64_retain` — retain turns 0.29_f64 into 0.2899… and Down-rounds to 0.28, a silent one-tick error (regression-tested); it clamps 0.0/1.0 to [tick, 1−tick] because late-window p_up legitimately saturates, but errors on non-finite/outside-[0,1]. (4) `quantize` clamping can override the rounding direction at the grid floor/ceiling (documented; callers needing strictness use `on_grid`). (5) Shared `taker_fee(size, rate, price)` helper in core-types (shares × rate × p × (1−p), 5-dp half-away rounding, $0.00001 floor) so venue-paper/engine/analytics can't diverge; verified against all §7 reference points. (6) `Event` bus enum is Clone-cheap (payloads Copy-small or Arc-wrapped; guard test pins `size_of::<Event>() ≤ 96` — measured 88 bytes, the inline Copy `ModelSnapshot` is the widest variant), deliberately NOT serde (journal persists the inner payload structs, which all are) and NOT non_exhaustive (new variants must force consumer matches). (7) Ids validate on construction AND on deserialize (`serde(try_from = "String")`): ConditionId = 0x+64-hex lowercased, TokenId = decimal string (256-bit, never an integer type). (8) `InventorySnapshot` stores derived fields (matched pairs, pair cost, signed excess, worst-case) with `derive()` as the single construction path. (9) `OrderState::can_transition_to` allows PendingCancel→Filled (fill races cancel). (10) Added `BreakerKind::EngineRestart` beyond the §11 list for the matching-engine-restart path. Deferred: NonZeroSize, UnderlyingPrice newtype, ids as Arc<str>. Integration: downstream crates consume flat re-exports (`core_types::{Price, Event, MarketInfo, …}`); whole workspace passes fmt/clippy `-D warnings`/tests.
- 2026-06-11 — `config` crate implemented + wired into `bot` (telemetry, CLI). **Dependency decisions:** figment 0.10 chosen over the crates.io `config` crate (allowlist offered either; figment avoids the crate-name shadow, has provenance-rich multi-errors and `Jail` for hermetic layering tests; features toml+env only, `test` dev-only). tracing-appender 0.2 added for the rolling file appender — not literally on the §3 allowlist but it is the official tokio-rs/tracing companion crate (same repo as tracing-subscriber, no new transitive weight beyond `time`/`crossbeam-channel`); treated as covered by the "tracing + tracing-subscriber" entry. anyhow + serde_json + thiserror + tracing stack added to `bot` only. thiserror pinned 2.0.17 / anyhow 1.0.100 in Cargo.lock: the operator's Windows Application Control intermittently blocks freshly-linked build-script exes (os error 4551, per-file-hash verdicts); pinning one patch down produced fresh binaries that passed — if 4551 recurs, retry the build or repin; harmless on Linux.
- 2026-06-11 — Config architecture decisions: (1) **Layering** = code defaults (`AppConfig::default()`, pinned byte-for-meaning to committed `config/default.toml` by a test) → `default.toml` (required; missing = hard error) → `bot.local.toml` (optional, matches existing `*.local.toml` gitignore) → `BOT_<SECTION>__<KEY>` env vars; `Toml::file_exact` for **both** file layers (the optional one behind an explicit existence check) — figment's `Toml::file` searches parent directories for relative paths and could silently merge a stray ancestor `config/bot.local.toml`. (2) **Secrets are env-only and not figment fields**: `SecretString` redacts in Debug/Display and implements **no Serialize/Deserialize** (compile-time guarantee the logged config tree can't contain one); six `BOT_SECRET_*` vars (dashboard token, PM api key/secret/passphrase, private key, live confirm); the loader filters `BOT_SECRET_*`/`BOT_CONFIG_DIR` out of the env layer and rejects a fixed list of secret-shaped dotted keys found in any remaining config layer — TOML files, or a mis-namespaced non-secret env override like `BOT_DASHBOARD__AUTH_TOKEN` (`SecretInConfigFile`, fails closed either way); `deny_unknown_fields` everywhere backstops. Empty env values = absent. (3) **Mode is a CLI subcommand, not a config field** — no file can push toward live; `live` section carries only `enabled=false` (gate 1); gate 2 = `BOT_SECRET_LIVE_CONFIRM` matching the **hardcoded** `LIVE_CONFIRM_PHRASE` ("arm-live-i-accept-real-money-losses"); gate 3 (dashboard arm) is a later task; `bot live` currently always refuses. (4) **Per-series params** = full `EngineParams` under `engine.defaults` + sparse all-`Option` `EngineParamsPatch` per series (`[engine.series.BTC-5m]`, `enabled` flag + `params`); resolved = patch over defaults; patch/params field-set parity is test-pinned; per-series env overrides are unsupported (env keys lowercase; series keys are case-sensitive) — TOML/dashboard only. Resolved params of **all six** series are validated, disabled ones included. (5) **Validation collects all violations** (`ConfigError::Invalid(Vec<Violation>)`, key+message each) across two phases: figment extract (types, unknown keys, multi-error) then semantic ranges/cross-field (min_edge ≥ one tick 0.01, no_passive ≤ no_atm ≤ window secs, vol floor < cap, staleness ∈ (0,500] per §11, touch/ladder sizes ≥ 5 shares per §7, non-loopback dashboard bind requires token, live.enabled requires all PM secrets + exact phrase — checked even in paper so a half-armed config fails at boot). (6) Decimals in TOML may be floats (rust_decimal shortest-round-trip; 0.07 → exactly 0.07, test-pinned) or strings. (7) §8/§9/§11 defaults chosen (config/default.toml): min_edge 0.01, k1 1.0, hold 30 s, γ 0.05, touch 10 sh, ladder 3×10 @1 tick, pair-cost 0.98, no-ATM 25 s, no-passive 5 s, θ 0.005/cancel-market 0.02, EWMA half-life 60 s, vol floor/cap 0.00002/0.005, taker buffer 0.005, late τ 30 s/certainty 0.97/cap 0.99, taker budget $10/window, cooldown 5 s, excess caps 50/100 sh, max worst-case loss $25/window; risk: staleness 500 ms, daily stop $200, open notional $1000, sanity 0.10 for 3 s, breaker 10 errors/60 s; paper: capital $10k, latencies 150+U[0,100] ms (one-sided upward jitter — pessimistic per §9), fee 0.07/rebate 0.20 as config defaults only. (8) **Telemetry in `bot`** (`telemetry.rs`, config crate stays tracing-free): console compact layer + non-blocking daily-rolling plain-text file layer (`data/logs/bot.<date>.log`, 14 kept), per-target `EnvFilter` from `RUST_LOG` else `log.default_filter`; `WorkerGuard` held in main; log dir pre-created (appender's max_log_files pruning stderr-complains on a missing dir). No JSON log feature (journal owns structured data). (9) No tokio yet — boot path is synchronous; no clap (hand-rolled ~40-line parser: `paper|live|check-config`, `--config-dir` > `BOT_CONFIG_DIR` > `./config`); no dotenv (`.env.example` is documentation; nothing auto-loads it). Acceptance verified: `bot paper` boots, logs redacted effective config (JSON of `AppConfig` + secret presence booleans), exits 0; `bot check-config` prints the same to stdout; invalid configs (negative capital, sub-tick spread) exit nonzero listing every violation. 48 config unit tests + 6 bot unit tests + 5 spawned-binary integration tests.
- 2026-06-11 — Adversarial multi-agent review of the config task; confirmed findings fixed: (1) optional `bot.local.toml` layer switched from `Toml::file` to `Toml::file_exact` + existence check — `Toml::file` searches ancestor directories for relative paths, so a stray `config/bot.local.toml` anywhere above the cwd would have silently merged into the trading config; (2) `.gitignore`'s `.env.*` pattern was swallowing the committed `.env.example` — added `!.env.example` negation; (3) added a spawned-binary test of the `paper` boot path (`BOT_LOG__DIR` → temp dir): exit 0, console + flushed rolling file both carry the boot sequence, secret values absent from stdout/stderr/log file; (4) `SecretInConfigFile` message/comment now covers env-sourced hits (a mis-namespaced `BOT_DASHBOARD__AUTH_TOKEN` trips the same probe — it was never file-only); (5) `build_filter` normalizes cosmetic whitespace/empty segments in directives (`"info, engine=debug"` no longer aborts boot); (6) empty `BOT_CONFIG_DIR` now counts as unset, matching the empty-secret convention. Triaged, no change: figment type-mismatch errors echo the offending file value to stderr by design (clear errors are an acceptance requirement; realistic secret keys are blocked pre-extract by the forbidden-key probe); anti-drift test pins values not key coverage (missing file keys correctly fall back to code defaults); dashboard `bind` port 0 is legal (OS-assigned); engine-defaults violations reported per-series is intentional (each series validates its resolved params against its own window length).
- 2026-06-11 — **Gamma/CLOB discovery facts verified live** (read-only probes; raw responses committed as fixtures under `crates/discovery/tests/fixtures/`). (1) The six series exist as Gamma *series* objects (`GET /series?slug=…`; `id` arrives as a JSON **string**): btc-up-or-down-5m=10684, -15m=10192, -hourly=10114; eth-up-or-down-5m=10683, -15m=10191, -hourly=10117. Ids are resolved from configured slugs at runtime, never hardcoded. (2) **Uniform window query** for all six series: `GET /events?series_id={id}&closed=false&order=endDate&ascending=true&end_date_min={now RFC3339}&limit={n}` → current window first, then upcoming. **`end_date_min` is mandatory**: stale events linger `closed=false` weeks past their endDate (observed a May 20 event on Jun 11) and sort first without it. Each event embeds exactly 1 market; windows are pre-created ~24 h ahead (`acceptingOrdersTimestamp`). (3) Event slugs: 5m/15m are deterministic `{btc|eth}-updown-{5m|15m}-{unix-open-secs}`; hourly slugs are human dates (`bitcoin-up-or-down-june-11-2026-4am-et`) — the series-id query is the only uniform path. (4) `market.eventStartTime` is the authoritative window open (event-level `startTime` is empty/absent in hourly list views; `market.startDate` is market *creation* time — never map it); `endDate` is the close; discovery enforces `close − open == series duration` per event. (5) `outcomes`/`clobTokenIds`/`outcomePrices` are JSON-encoded strings *inside* the JSON (double-encoded parallel arrays); Up/Down token mapping is by label, never position. (6) `feeSchedule {rate 0.07, exponent 1, takerOnly true, rebateRate 0.2}` + `feesEnabled` (feeType `crypto_fees_v2`) is the authoritative fee descriptor; the flat `makerBaseFee`/`takerBaseFee`=1000 and `makerRebatesFeeShareBps`=10000 fields contradict it and are ignored. (7) **Resolution sources differ by duration: 5m/15m resolve on Chainlink data streams (`data.chain.link/streams/{btc|eth}-usd`); the 1h series resolve on the Binance {BTC|ETH}/USDT 1H candle (close ≥ open ⇒ Up)** — §6's store-per-market rule proved necessary; `MarketInfo` now carries a parsed `ResolutionSource`. (8) CLOB `GET /markets/{cid}` (no auth) is used as a cross-check (min size/tick/neg_risk/token labels) but its `end_date_iso` is date-truncated (`…T00:00:00Z`) — Gamma is the timing authority. (9) Tick can already be 0.001 at discovery (observed live on drifted markets, including both hourlies mid-window) — `orderPriceMinTickSize` is read per market, never assumed 0.01.
- 2026-06-11 — Dependencies added for discovery (all §3-allowlisted): **reqwest 0.12** (default-features off, `rustls-tls` only; no `json` feature — bodies decode via `serde_json::from_slice` so errors carry url/status/body-prefix context), **tokio 1** (workspace base feature `rt`; bot adds `net`+`time` for the current-thread runtime; discovery uses it dev-only for `#[tokio::test]`), **time 0.3** (`std`/`parsing`/`formatting`, RFC3339) — settles the §3 "chrono or time" choice; already transitive via tracing-appender, zero new tree weight. JSON-number decimals (0.01/0.001/0.07) decode via rust_decimal shortest-round-trip — same precedent as config's TOML floats, pinned by a test; serde_json `arbitrary_precision` rejected (deserialize_any interop hazard + workspace-wide feature unification).
- 2026-06-11 — `discovery` crate implemented + `bot discover` wired. core-types additions: `ResolutionSource{kind,raw}`/`ResolutionKind`, extended `FeeParams` (rate/exponent/taker_only/rebate_rate/enabled), `MarketInfo.event_slug` (human/URL window identity — `polymarket.com/event/{slug}` — for dashboard/journal/operator verification). New `[discovery]` config section: six series slugs (env-overridable, validated non-empty/no-whitespace/unique), `http_timeout_ms` 5000, `lookahead` 2 (upcoming windows held beyond current; query limit = lookahead+1; validated ∈ [1,10], ≥1 per §6). Crate layering: permissive `wire` structs (no deny_unknown_fields — the APIs add fields freely) → fail-closed `map` (missing feeSchedule/feesEnabled/negRisk/outcomes ⇒ MapError ⇒ the whole series refuses to trade) → `DiscoveryApi` trait seam (async-fn-in-trait/RPITIT, no async-trait or httpmock crates; service tests run a FakeApi over the committed fixtures) → clock-free `DiscoveryService` (`now` is always a parameter; scheduler will own timing/retry/the §6 ≥60s-before-close contract; `refresh_all` isolates per-series failures). CLOB cross-check always-on per refresh: strictest min size (max), **coarser** tick on mismatch (every 0.01-grid price is on the 0.001 grid, so the coarser grid is the safe subset; live `tick_size_change` corrects later), token/neg_risk disagreements keep Gamma + warn. Acceptance verified live: `bot discover`/`just discover` printed all six series (current + 2 upcoming each) with slugs, times+countdowns, condition ids, full Up/Down token ids, tick, min size, fee descriptor, resolution source; exit 0; BTC-5m row cross-checked against the polymarket.com event page (title/window times/Chainlink rules matched); consecutive runs tracked the clock across a 5m boundary; a bogus-slug override produced an ERROR row + exit 1 with the other five series unaffected. Workspace: fmt + clippy −D warnings + 187 tests green.
- 2026-06-12 — `scheduler` crate implemented + `bot schedule` smoke subcommand. **§6 reinterpretation (docs win, recorded per §2.8):** the market-channel docs (verified 2026-06-11) show `market_resolved` fires *after* close and `new_market` carries **no window open/close times**, while windows are contiguous back-to-back — so the WS events physically cannot be the roll trigger. Implemented semantics: **the wall clock (against discovery-supplied window times) is the deterministic roll driver; WS events are redundant accelerants** — `new_market` = refresh hint, `market_resolved` = outcome for the already-rolled window. §6's intent (a missed event can never stall a series) is preserved; rolling on `market_resolved` would gap every window. **Roll-at-close + park:** at close the machine announces `Closed` and immediately opens the successor; the closed window parks in an awaiting-resolution set (cap 8, oldest evicted) until `market_resolved` maps `winning_asset_id` → outcome via `TokenPair::outcome_of` (label field deliberately ignored; unknown winning token ⇒ warn, never fabricate an outcome). core-types additions: `WindowLifecycle::Closed` (emission contract `Discovered → Open → Closing → Closed → Resolved`, `Resolved` possibly after the successor's `Open`) and `MarketLifecycleEvent{NewMarket, MarketResolved}` — the feed-clob seam (Day 3), defined in core-types because feed-clob depends only on core-types; it is scheduler *input* on a dedicated channel, not a bus `Event` variant. **Architecture:** sans-IO `SeriesMachine` (clock-free/IO-free/log-free; inputs `on_tick`/`on_discovery`/`on_market_event`, outputs as values — `Announce`/`Refresh{reason}`/`Warn(CoverageWarning)`; catch-up loop replays oversleeps across multiple boundaries in order) + one async driver task owning all six machines; discovery HTTP lives in a worker task behind bounded mpsc so a slow refresh (~20 s worst case) can never delay a wall-clock roll; driver sleep = min(next deadline, 1 s) — the 1 s cap bounds clock-step/suspend damage. Bus = bounded `tokio::sync::mpsc::Sender<Event>` (cap 256, awaited send = backpressure); fan-out later in the orchestrator. Robustness: 45 s in-flight-refresh watchdog (lost request/wedged worker), ≤1 refresh in flight per series, 1 s `new_market` hint spacing, per-series isolation throughout (driver errors only on dead bus/worker). **Timing policy** (new `[scheduler]` config section; §4 row gains the config dep — already transitive via discovery, zero rebuild cost): refresh on every window open (`Rolled`) + pre-close verify at close−`refresh_lead_ms` (120 s) + heartbeat at `max_refresh_interval_ms` (600 s); failures/unsatisfying results retry at backoff 1 s→×2→30 s cap (backoff NOT reset by a success that still lacks the next window — a long venue gap must not hammer Gamma at 1 s); `ContractViolated` fires once per window at close−`next_window_lead_ms` (60 s floor, validation-enforced); `Closing` at close−`closing_lead_ms` (30 s); `resolution_timeout_ms` 120 s (overdue warnings suppressed until a market-event source is attached: `expect_resolutions` = `market_rx.is_some()`). tokio features: scheduler `rt/sync/time/macros` (+`test-util` dev for `start_paused`), bot adds `sync/signal/macros`. Tests: 23 scheduler (deterministic machine matrix incl. the two acceptance tests — 3 consecutive zero-gap rolls; missing-window gap with backoff pacing, single §6 warning, loud gap at close, recovery + backoff reset — plus paused-time driver tests over a `Refresh` trait fake; no wire fixtures needed — discovery owns the API boundary, scheduler consumes validated `SeriesWindows`). Live smoke (`just schedule`, read-only, 34 min, 2026-06-11 20:01–20:35 UTC): all six series discovered + opened within ~5 s of boot; 92 lifecycle announcements; 18 rolls exactly on schedule (7 per 5m series, 2 per 15m; 1h windows closed outside the run); **all 36 boundary events (Closed + successor Open) landed within 3–43 ms of the wall-clock boundary** with the Open ≤1 ms after its Closed; zero warnings/errors of any kind (no §6 ContractViolated, no NoNextAtClose); final coverage table: every series active, next window known, refresh ages 14–314 s (under the 600 s heartbeat), parked counts exactly the unresolved closes (7/7/2/2/0/0) — resolutions arrive when feed-clob lands. Windows App Control note: with ~19 test exes, one `cargo test --workspace` invocation now almost always trips a 4551 on some exe (per-*execution* scanner race, not per-hash); the reliable local gate is per-package `cargo test -p <crate>` with retries — all 17 packages green, 217 tests. Irrelevant on Linux.
- 2026-06-12 — `timeutil` implemented (clock/tau/stats/sntp/offset/skew/monitor + feature-gated `harness/`, 47 unit tests incl. the mocked-clock acceptance test) + `bot latency` subcommand + `[clock]`/`[latency]` config sections. **Dependencies added (§3):** tokio-tungstenite 0.29 (default-features off; `connect/handshake/stream/rustls-tls-webpki-roots` — same rustls 0.23 line and webpki root store as reqwest, single rustls verified via `cargo tree`); futures-util 0.3 (`std`+`sink` only) — transitive of tokio-tungstenite anyway, needed to drive `WebSocketStream`'s Stream/Sink. Harness (reqwest+tungstenite) is behind timeutil's `harness` cargo feature so future model/engine consumers of Clock/τ never rebuild network deps; per-package testing needs `cargo test -p timeutil --features harness`. **Clock abstraction:** `Clock { mono() -> Duration, wall() -> TimestampMs }`, `SystemClock` + public `MockClock` (`advance` moves both, `step_wall` injects skew); free `wall_now()` clamps instead of erroring (pre-epoch → 0); bot's `system_now_ms`/`schedule::wall_now` migrated to it; scheduler keeps its `Fn() -> TimestampMs` seam. **SNTP hand-rolled** (no allowlisted NTP crate; RFC 4330, one 48-byte packet over tokio UDP): offset/delay math in wrapping 32.32 fixed point (era-safe past 2036, regression-tested); fixed→ms conversion **rounds to nearest** — double truncation systematically loses 1 ms (350 reads back as 349; regression-tested); validation = mode 4, LI≠3, stratum-0 kiss-of-death surfaced, originate must echo our T1; T1/T4 from the wall clock by design (the per-round median absorbs a mid-query step). **Skew policy (§11):** trip at |offset| ≥ 250 ms for 2 consecutive rounds, clear below 125 ms for 3 consecutive, dead zone between the bounds resets both streaks; 60 s cadence with a 2 s-spaced boot burst sized `trip_after`; **a failed NTP round never trips and never clears** (a lost UDP packet says nothing about the local clock; §11 trips cost a cancel-all; ~50 ppm crystal drift needs >80 min to reach 250 ms) — failures warn, escalating to a once-per-period staleness alarm at 300 s (UDP/123-blocked VPS ⇒ permanent staleness ⇒ deployment blocker, visible per-region in the harness report). Sans-IO `SkewMonitor` + driver emitting `Event::Risk(BreakerTripped/Cleared { ClockSkew })`; reacting (cancel-all) is the future risk manager's job. CLOB `GET /time` returns whole unix **seconds** → report-only venue cross-check, never an alarm input. **API facts verified live (RTDS docs contradicted; §7 updated per §2.8):** `crypto_prices` ignores the docs' plain-string `filters` (empty-frame ack, then silence); the JSON-string form (`"{\"symbol\":\"btcusdt\"}"`) works — empty ack, one 2-min backfill message, then ~1 update/s per symbol. CLOB market channel: subscribing to all 12 live tokens floods ~1500 msg/s and the server RSTs the socket mid-measurement; one token pair survives the full window — the harness subscribes to the first live window's Up/Down pair only and sends the text PING every 5 s (docs require ≤10 s; halving avoids grazing the deadline). **Harness:** report schema v1; bot owns the file write (`data/latency/latency-{label}-{YYYYMMDD-HHMMSS}.json`, `--out` overrides; timeutil returns a value and stays filesystem-free); REST RTTs are warm-pool (warmup excluded) = the SDK order path's steady state; a WS section is OK iff it produced gap stats (early disconnect after real data is recorded but non-fatal; a silent socket fails); market-channel gaps labeled activity-dependent — RTDS is the cross-region-comparable gap metric. **Live acceptance (dev PC, 2026-06-11):** exit 0; REST p50 ≈ 180 ms / p95 ≈ 191 ms (to eu-west-2 per docs trading/overview); market channel 49.6k msgs over a clean 60 s; RTDS 60 msgs/60 s (gap p50 1015 ms = the 1 s cadence); NTP offset **−963 ms: the operator's Windows clock is ~1 s ahead** (corroborated by the CLOB /time delta) — the subsequent `bot schedule` smoke demonstrated the full §11 path live: breach warning on round 1, trip on round 2, `RISK EVENT: BreakerTripped { ClockSkew }` on the bus within ~5 s of boot. **Operator action: resync the Windows clock** (Settings → Time → Sync now), else every paper session will boot tripped. 4551 addendum: hash-sticky blocks on fresh test exes recurred; the reliable unblock is a trivial comment edit in the affected crate (mints a new exe hash), then retry.
- 2026-06-12 — `feed-rtds` implemented (wire/sub/machine/backoff/transport/driver, 56 tests incl. 10 paused-time driver tests over a scripted fake transport + 10 fixture tests over committed real frames) + `bot feed` subcommand (`--raw <file>` taps every in/out frame to JSONL — the fixture-capture mechanism) + `justfile feed` recipe. **core-types:** new Copy-small `Event::FeedHealth(FeedHealth::{Stale, Recovered})` carrying (source, asset, age/gap) — deliberately not a `RiskEvent`: per-stream granularity for model/dashboard, and breaker authority (`FeedStale` + cancel-all, §11) stays with the future risk manager (single writer; skew-monitor precedent is feeds-report/risk-reacts). **Config:** `feeds.rtds_stale_after_ms` = 5000 (RTDS cadence is ~1 Hz; validated > 1000). **Dependency:** `rustls` 0.23 declared explicitly (`ring/logging/std/tls12`, §3 TLS policy, transitive either way) — tokio-tungstenite alone selects no process CryptoProvider, so any binary without reqwest panicked at first TLS connect; `WsTransport` now pins ring via `install_default` in a `Once`. **RTDS wire truths beyond §7's update (all live-verified, pinned by `tests/live_probe.rs` + committed fixtures):** one filter slot per (connection, topic) with replace-not-add semantics killed the naive 4-filtered-subscribes design (only the last symbol per topic stayed live); steady state = all-symbols subscribe + client-side filtering; filtered subscribes still sent first on every (re)connect because each triggers a ~2-min backfill (~60–120 points) that pre-seeds the vol EWMA; backfill shape is `{symbol, data:[{timestamp,value}…]}` with the chainlink one arriving under the WRONG topic — identity is symbol-resolved everywhere (`map_symbol`), topic only gates frame relevance; live updates carry `full_accuracy_value`, adopted via a self-validating ladder (accept direct or ÷10¹⁸ interpretation only when within 0.1% of the float `value`, else fall back to the float — a future format change degrades gracefully); subscribes can 500 (`{body:{message},statusCode}`, classified `IgnoredReason::ServerError`) — observed killing chainlink btc/usd for an entire session while eth/usd streamed. **Self-healing:** per-stream staleness watchdog (sans-IO `FeedMachine`, ages on local receive time; disconnect ⇒ immediate `Stale` for all; recovery per stream on first tick) + driver-level dead-socket teardown at 3× ping interval + **connection recycle when any tracked stream starves 6× stale_after within one episode** (= 30 s default; starvation measured from the per-episode anchor so recycles can't loop) — directly fixes the observed 500-starved-stream failure, since a fresh connection's resubscribe recovers (probe-verified). **Other decisions:** backfill ticks published with honest old `ts_exchange` (consumers judge recency; feed never fabricates freshness); equal-jitter backoff (`raw/2 + U[0, raw/2]`, hardcoded policy) over a 15-line xorshift64* seeded from the clock — `rand` stays off the tree; untracked symbols (all-symbols subscription) skip at trace, all other skip reasons WARN through a 10 s-suppressing `WarnGate`; bus events: ticks as `Event::PriceTick`, health as `Event::FeedHealth`; transport behind an RPITIT seam (`Transport`/`Connection` + crate-local `WsFrame`) with `TapTransport` decorator for `--raw`. No text "PONG" reply observed from RTDS in a 1556-frame session (the parser's Pong arm is market-channel-style defense). Fixture provenance documented in `crates/feed-rtds/tests/fixtures/README.md`. **Live acceptance (dev PC, 2026-06-12, ~8 min, RTDS server visibly flaky that night):** all four streams live side-by-side with sub-second receive ages and ~1 s exchange ages; the server 500'd subscribes repeatedly, starving individual streams — the recycle fired at exactly 30 s each time (4 recycles), every stream recovering on the fresh connection (`recovered (gap 31.1s)`); a 25 s process freeze (NtSuspendProcess — no PINGs sent) surfaced as a 26 s gap with clean per-stream Stale→Recovered and no spurious dead-socket trip (buffered frames drain before the watchdog tick); a 75 s freeze forced a genuine server-side connection drop → automatic reconnect + resubscribe-all + per-stream recovery. Ended with reconnects=5, all live. The §11 risk reaction to `FeedHealth` (cancel-all via the risk manager) and the model's consumption of backfill seeds are later tasks. Note: per-package `cargo test` tripped 4551 once; comment-edit remint cleared it.

- 2026-06-12 — **`feed-util` extracted from feed-rtds** (new §4 crate, mandated by the feed-binance task: shared WS plumbing, not duplicated). Moved verbatim: transport seam (`Transport`/`Connection` RPITIT, `WsTransport`, `TapTransport`), jittered backoff, `FeedError`; genericized: the sans-IO staleness machine (now keyed by a `StreamKey` trait — `source()/asset()/kind()` — with **per-key `stale_after`**) and the driver loop (now parameterized by a `FeedProtocol` trait: keys+thresholds, connect messages, frame classification into `FrameOutcome`, runtime commands as ordered `CommandAction`s; keepalive is `Option<Keepalive>` in `DriverParams`, `dead_after` explicit, starvation recycle = 6× each key's own threshold). feed-rtds kept its full public API (`RtdsArgs`/`RtdsParams`/`run` + re-exports + `FeedStatus`/`KeyStatus` aliases) as a protocol impl + thin wrapper — **all 10 paused-time driver tests passed with identical timing/sequence assertions** (the extraction-correctness gate), 23 unit + 10 fixture tests green. The scripted fake transport moved into `feed-util` behind a `fake` feature (dev-dep for both feed crates; gained a `url_log` for pinning URL-based subscription). Two deliberate behavior deltas, both logged here per §2.8: (1) **driver log target changed `feed_rtds` → `"feed"` + `feed="rtds"|"binance"` field** (tracing targets are static; a generic driver cannot emit per-feed targets — README updated, `RUST_LOG=feed=debug` now covers both feeds); (2) **`WsFrame::Ping` surfaces from the transport** (liveness-only, not tapped, auto-pong still handled by tungstenite) so Binance's server pings count for dead-socket detection. Non-UTF-8 binary frames now lossy-decode and classify through the protocol parser instead of a driver-special-cased warning (same gating, same outcome). core-types: **`TickKind { Vendor, Mid, Trade }` added to `PriceTick` and both `FeedHealth` variants** (RTDS = Vendor; sizes re-verified: Event 88 ≤ 96, FeedHealth ≤ 32) — needed so health/diagnostics can distinguish a quiet trade stream from a dead book stream and so the comparator can identify what a vendor feed republishes; `PriceSource`/`TickKind` gained `Ord`.
- 2026-06-12 — **`feed-binance` implemented** (wire/sub/protocol/driver + `compare` module; 22 unit, 8 paused-time driver, 6 fixture tests; fixtures captured live — provenance in `crates/feed-binance/tests/fixtures/README.md`) + `bot feed --source rtds|binance`, `bot compare`, justfile `feed-binance`/`compare`. **Binance WS facts (docs read + live-verified 2026-06-12, 10k-frame capture):** combined-stream URL `{base}/stream?streams=btcusdt@bookTicker/btcusdt@trade/…` subscribes by itself — **zero client frames ever** (pinned by capture: 9 992 in / 0 out; satisfies the 5-inbound-msgs/s limit by construction); frames arrive wrapped `{"stream","data"}`; `bookTicker` payload `{u,s,b,B,a,A}` has **no event time** → published as bid/ask midpoint (`TickKind::Mid`) with `ts_exchange := ts_local` (documented: NOT a latency reading); `trade` payload has `E`/`T` ms → published at `p` with `ts_exchange := T` (`TickKind::Trade`); payload symbols uppercase, stream names lowercase; server pings every 20 s (tungstenite auto-pongs; they surface as liveness → `DEAD_AFTER` = 60 s const); 24 h connection limit = ordinary reconnect. Config: `binance_ws_url` default now the **base** `wss://stream.binance.com:9443` (`data-stream.binance.vision` documented as drop-in); new `binance_stale_after_ms` 2500 (≥ 500) and `binance_trade_stale_after_ms` 30000 (≥ book threshold) — per-stream-kind thresholds exist because trade prints legitimately gap seconds in quiet markets and a single threshold would flap `FeedHealth` (driver test pins quiet-trades-never-alarm). Zero/negative-priced bookTicker sides are rejected, never published.
- 2026-06-12 — **Feed-comparison measurements (live, 20-min `bot compare` run, dev PC)** — the §8 basis/lead-lag groundwork; comparator = sans-IO `feed_binance::compare` (ts_local-only timing; 100 ms LOCF grid; basis in bps; ±5 s best-lag xcorr of 1 s log-returns; exact-Decimal match lag on value *transitions*; constants are code, not config — diagnostic, not engine path). Findings with RTDS healthy: (1) **RTDS `crypto_prices` republishes Binance LAST-TRADE prices at 1 Hz, ~0.7 s late** — direct:trade→rtds-binance match rate 99–100 % at p50 705–724 ms (p95 ~1.26 s); direct:mid matched only 7–33 % (incidental); xcorr agrees (direct:mid leads rtds-binance 700–800 ms, corr 0.78). (2) **Direct Binance leads Chainlink by ~2.7–2.8 s** (rtds-binance leads chainlink ~1.9–2.0 s) — that lead is the strategy's signal-edge window over the resolution feed. (3) **Chainlink-minus-Binance basis was ≈ −13.3 bps that window** (Binance above; direct-vs-chainlink and rtds-vs-chainlink agreed within 0.1 bps — internal consistency) — material vs our 1-tick edges; confirms §8's basis-corrected fair value design. (4) Rates: BTC bookTicker ~80–120 mid-ticks/s + ~25–37 trades/s; both RTDS topics exactly 1.0/s/symbol. Run also live-exercised the extracted driver end-to-end: RTDS's recurring subscribe-500 starved streams 3× → stale at threshold → **starvation recycle at exactly 6×stale_after → 162 ms backoff → reconnect → per-stream recovery** (12 stales / 12 recoveries), Binance side 0 reconnects — the §11-relevant resilience path proven on the shared loop. Deliberate non-test: no forced network kill (operator's PC; the paused-time suites + the real venue failure cover reconnect). Note for the model task: consume `TickKind::Mid` from `BinanceDirect` as the fast signal; treat `BinanceRtds` as redundant (same data, 0.7 s later); basis-correct against `ChainlinkRtds`.
- 2026-06-12 — **`feed-clob` implemented** (wire/book/machine/driver/supervisor; 41 unit + 10 paused-time supervisor tests + 12 fixture tests over captured real frames + an `#[ignore]`d capture-replay diagnostic) + `bot ladder` subcommand (justfile `ladder`) + **first `market_rx = Some(...)` wiring anywhere** (scheduler `expect_resolutions` now live in that run mode). **Topology: one WS connection per window (token pair), never dynamic resubscribe** — §5 + the 12-token RST datapoint; the supervisor consumes scheduler `Event::Window` announcements (forwarded by the binary over a typed mpsc — bus fan-out stays an orchestrator concern), pre-connects the successor at `open − clob_presubscribe_lead_ms` (90 s default) so rollover is gap-free by construction, lets a closed window linger `clob_resolution_linger_ms` (180 s) for its `market_resolved`, and TTL-dedups (10 min) market notices across connections before forwarding to the scheduler. **feed-util's generic `FeedProtocol` driver was NOT reused** (price-tick-shaped: `PriceObs`/`Event::PriceTick`; the staleness machine keys on PriceSource/Asset/TickKind) — feed-clob hand-rolls its connection loop over the shared `Transport`/`Backoff`/`FakeTransport` seams; `Backoff` made `pub` in feed-util for this. feed-clob deps add tracing + tokio rt/time/macros (it spawns tasks and logs itself, unlike feed-rtds/binance whose loop lives in feed-util).
- 2026-06-12 — feed-clob design decisions: (1) **Venue `book` snapshots are ground truth and replace local state wholesale**; the delta-maintained book is audited against every arriving snapshot (drift counter; ~1% of snapshots drift without an observed trade — trade prints can arrive after their snapshot, partial fills change sizes without deltas — repaired by the replace, never a recycle trigger). (2) **Crossing deltas are trades at the touch, resolved by implied consumption** (`BookState::apply_change` drops opposite-side levels at-or-better than the new price; `ChangeOutcome::Applied{consumed}` is telemetry) — the first live run recycled on every such trade until this was diagnosed from the raw capture; `BookUnreliableReason::Crossed` now means a venue snapshot itself arrived crossed (latch only, no recycle). (3) Connection recycling reserved for what snapshots cannot fix: derived-top vs venue-top divergence past a 2 s grace (code constant), active-window book silence past `clob_book_stale_after_ms` (10 s; quiet-market adaptive: a stale recycle whose fresh snapshots are content-identical doubles the effective threshold, capped 6×, reset on the first genuine delta; pre-open/post-close exempt), dead socket at 3× ping. (4) Bus publishing: `TopOfBook` immediately on real top change, `LastTrade`/`TickSizeChange` immediate, full `Event::Book` immediate on snapshots and coalesced to `clob_book_publish_interval_ms` (200 ms) for delta-built books (carrying `seq_hash: None`; only venue snapshots carry the hash). (5) New `Event::BookHealth` (per-window, latched Unreliable{Disconnected|Stale|Crossed|TopDivergence}/Recovered, boot trust silent, every Recovered paired with exactly one announced Unreliable) — feeds report, the future risk manager reacts (§11). (6) Wire parser is lenient: string-or-number numerics, single-object OR array frames, `"0"`/`""`/absent venue tops → None (and a tops report of (None, None) is treated as "no report" — the empty-side encoding is ambiguous), per-event failures inside an array don't poison siblings. (7) Five `[feeds]` keys added (`clob_ping_interval_ms` 5000, `clob_book_stale_after_ms` 10000, `clob_presubscribe_lead_ms` 90000, `clob_book_publish_interval_ms` 200, `clob_resolution_linger_ms` 180000).
- 2026-06-13 — feed-clob live acceptance (dev PC, BTC-5m): an unplanned **8.5-hour endurance run** (started 2026-06-12 09:13 UTC) with the implied-consumption fix: **recycles 0, anomalies 0** on every live connection, ~100 window rollovers each landing `Open` exactly at the :00 boundary with the successor pre-connected (2 connections per series in the lead window — gap-free by construction), `market_resolved` → scheduler `market_rx` → parked-window `Resolved` announcements flowing the whole run (`RESOLVED <slug> -> Up|Down` in the ladder events pane), old connections torn down on own-resolution sighting. Forced reconnect (`--recycle-after 90`): Unreliable{Disconnected} → reconnect → fresh snapshots → Recovered within ~1 s, episodes 2, no exit. Ladder verified against the CLOB REST `/book` at the same instant: **top-of-book prices matched exactly both sides** (sizes churn at ~100 msg/s — same magnitude). Fixtures committed from a 424k-frame capture (`crates/feed-clob/tests/fixtures/`, provenance README): every event type captured real EXCEPT tick_size_change (no flip occurred — synthesized from docs, marked, replace when captured); session-replay fixture test pins the snapshot-equality invariant (drift < 10% of audited snapshots; observed ~1%) and crossed == 0. Capture findings folded into §7 (PING/PONG, array frames, trades-only-via-snapshots, crossing-delta semantics, mirror pattern, new_market platform-wide broadcast, market_resolved field shape, per-pair load). Windows 4551 note: freshly built bot.exe blocked repeatedly this session; deleting the blocked exe + rebuilding, or one trivial-edit remint cycle, cleared it each time.
- 2026-06-13 — **`model::vol` implemented** (first model-crate module; deps added: rust_decimal + thiserror, both workspace/allowlisted) + `bot vol` smoke subcommand + justfile `vol` recipe + README section. Per-asset, per-(source,kind) EWMA variance of 1-second log returns; sans-IO (no clock/log/alloc on the tick path; time enters only via tick timestamps); 13 unit tests. Decisions: (1) **Bars keyed by `floor(ts_exchange/1000)`** — each estimator pins one (source, kind) so every differenced timestamp shares one clock domain; the RTDS backfill (~60–120 points 1 s apart in exchange time, delivered in one local burst) seeds warmup instantly; identical to ts_local on the default BinanceDirect/Mid input (bookTicker has no event time). Older-than-open-bar ticks are dropped (`OutOfOrder`) — this is also what discards a reconnect-backfill's replayed overlap, preventing double-folds; plus a glitch guard rejecting ts_exchange > ts_local + 10 s (code const) so one future-stamped tick can't poison the bar high-water mark. (2) **Bias-corrected EWMA** (`acc`/`wsum` pair, var = acc/wsum) instead of seeding with the first sample — at HL 60 a direct seed still carries weight λ⁶⁰ = ½ after 60 folds, i.e. half the "warmed" estimate would be one χ²(1) sample; with normalization the first sample's weight at warmup end is ~1.2%. (3) **Gap-aware fold** `var ← λᵏ·var + (1−λᵏ)·(r²/k)` with λ = 2^(−1/half_life) per second (E[r²/k] = σ²_1s); gaps > `max_gap_secs` (default 30) re-anchor the chain WITHOUT folding and leave acc/wsum/count frozen (a single outage-spanning return would get weight ≈ 1 and wipe the estimate; decaying wsum without samples drives it toward 0/0) — staleness trust stays with FeedHealth/risk per the feeds-report/risk-reacts precedent. (4) Warmup = `warmup_returns` folded samples (default 60); backfill counts by design. Quality is two-state {NotReady, Ready}; Unreliable belongs to the future model-health driver. (5) Engine consumption path is `sigma() -> Option<f64>` — `Some(clamped σ)` ONLY when Ready, so a warming estimate can't leak into fair value; `estimate()` exposes warming sigma + `Clamp {Inactive, Floor, Cap}` for dashboard/smoke. (6) Config: half_life/floor/cap map from the existing `engine.defaults` fields at the binary boundary (`bot/src/vol.rs::vol_params`, feed.rs precedent); input source/kind + warmup_returns/max_gap_secs are `VolParams` code defaults (BinanceDirect/Mid per the 2026-06-12 compare findings) — config exposure AND the per-series→per-asset params resolution are deferred to the model-driver task. Tests pin: constant-vol recovery (σ_true 5e-4, tolerance derived from the EWMA's ≈5.4% steady-state rel-std), spike excess halving at exactly HL **on variance** (|ratio−½| ≤ 1e-6 after wsum saturation; σ would not halve — sqrt is nonlinear), floor/cap engagement, warmup gating, backfill-burst seeding + replay dropping, λᵏ gap arithmetic, re-anchor frozen-state, intra-second collapse, wrong-stream/bad-value/glitch-ts rejection, params validation. **Live acceptance (`bot vol`, 4.2 min, dev PC 2026-06-12 18:36 UTC):** chainlink lanes seeded 57 backfill bars within ~1 s of connect; BTC binance:mid hit READY at exactly fold 60; all four lanes ended READY at σ ≈ 0.36–0.6 bps/√s (well inside floor/cap, clamp Inactive), reanchors 0; the run caught RTDS's known subscribe-500 live — BTC chainlink starved post-backfill → feed-rtds starvation recycle → reconnect's fresh backfill healed the 32 s outage (gap seconds folded at k ≤ 30, no re-anchor) with 26 replayed points dropped as OutOfOrder (ETH chainlink: 54) — the no-double-fold path proven against the real venue. Run was terminated externally after 250 s; the ctrl-c drain path is compare.rs's verbatim, covered by its prior acceptance. ModelSnapshot emission, ModelHealth wiring, basis tracker, and fair value are later tasks.

- 2026-06-13 — **Fair-value engine implemented** (`model::{math, strike, basis, fair}`, 39 new unit tests) + `bot fair` subcommand + `discovery::event_by_slug` verification seam + justfile/README. The §8 pricing brain: `p_up = Φ(z)`, `z = ln(S/K)/(σ_1s·√τ)`. **Strike source verified live (the §2.7 ambiguity, resolved):** Gamma market/event objects carry **no live strike** during a window (full key-list inspected). The venue publishes `eventMetadata.priceToBeat` (+ `finalPrice`) on the event only **~1–4 min after resolution** (absent 51 s post-close, present 4 min later); chain continuity is exact — `priceToBeat(N) = finalPrice(N−1) =` the UI history feed's `closePrice(N−1)` — so the venue samples its own Chainlink stream at the boundary instant. ⇒ **runtime self-captures the strike from RTDS Chainlink ticks; the post-hoc `priceToBeat` is the verification anchor.** Confirmed live in the `bot fair` acceptance run: BTC-5m window `1781320800000` froze K = 63568.22370757354 at **offset 0 ms** (a Chainlink print landed exactly on the open boundary), recovered from the RTDS backfill burst even though the bot booted ~48 s into the window. **Decisions:** (1) **Φ via Cody's CALERF** rational erf/erfc (three-interval, ~1e-16; routed `Φ(z)=½·erfc(−z/√2)` for tail accuracy) — hand-rolled (SNTP precedent, no new dep), `#[allow(clippy::excessive_precision)]` on the published coefficient block; pinned vector table passes at 1e-12 rel incl. Φ(−10)≈7.6e-24. (2) **`ModelSnapshot` extended** with `z`/`sigma_tau`/`anchor: AnchorSource{Chainlink,BinanceCorrected}` (the engine's repricing + the |fair−mid| breaker read them off the bus); `basis` field **redefined from "underlying units" to bps** (it was unused); `z` clamped to ±39 so JSON stays finite. **Event size guard bumped 96 → 128** (measured 104; still `Copy`-cheap) — the guard comment already invited deliberate tuning. (3) **Strike capture is settle-gated and arrival-order-independent** (`model::strike`): two monotonically-refined candidates (`before` = latest ≤ open, `after` = earliest ≥ open) freeze only once a *live* (recent exchange-time) at-or-after-open print proves wall-clock passed open and the backfill burst settled — freezing on the first within-tolerance print would be order-dependent under a reordered burst (a 3-ordering convergence test pins this). Rule default `LastAtOrBefore` (matches the verified chain continuity / LOCF-at-boundary); post-freeze improvements emit `RevisedCandidate` for journaling but **never move K**. `for_market` seam picks the feed/rule from `ResolutionKind` (Chainlink→tested; Binance-candle 1h→`FirstAtOrAfter`, provisional/unverified; Other→error). (4) **Basis tracker** (`model::basis`): bias-corrected EWMA of the **log basis** `b = ln(P_cl/P_binance_mid)` (additive in z; bps for display), HL 120 s, warmup 30, decay-clamp 30 s. The **backfill guard is the load-bearing difference from strike**: strike *wants* the RTDS backfill, the basis must *reject* it (a minutes-old Chainlink print paired against the current Binance mid would inject a huge spurious basis) — only live Chainlink ticks fold. (5) **Anchor + health** (`model::fair`): anchor = basis-corrected Binance when the fast feed is fresh AND the basis warmed, else fresh Chainlink, else none. `Warming` (strike/σ/Chainlink missing) / `Unreliable` (Chainlink age > `feeds.rtds_stale_after_ms`, OR divergence > 50 bps sustained ≥ 10 s — persistence filters the legitimate ~2.7 s Binance-lead divergence) / `Healthy`. τ=0 saturates to the ≥-ties-Up outcome; S=K ⇒ Φ(0)=0.5 (no tie adjustment, §8). Per-asset state (vol, basis) lives OUTSIDE the per-window engine so a rollover never resets σ/basis. (6) **Config**: zero new keys — `feeds.{rtds,binance}_stale_after_ms` map into `FairParams`/`BasisParams` at the binary boundary; vol params via the shared `vol_params`; divergence/basis-HL/heartbeat are model-crate consts (vol precedent). (7) **`bot fair`** wires scheduler+feed-clob+rtds+binance on one bus (ladder+vol topologies merged); recomputes per tick + 100 ms heartbeat; publishes `Event::Model` via `try_send` (the loop drains its own bus); 250 ms in-place render; calibration JSONL → `data/calibration/fair-{series}-{stamp}.jsonl` (snapshot/strike/outcome/verify records, generic `spawn_jsonl_writer`); post-hoc verification spawns a `discovery::HttpClient` per resolved window, polls `event_by_slug` (8×30 s), and reconciles `priceToBeat` vs our captured candidates (`exact`/`mismatch`/`unavailable`, rel-tol 1e-9 since Gamma numbers decode through f64). **Deps added to `bot`** (both §3-allowlisted): `serde` (derive, for `CalRecord`) and `rust_decimal` (book-mid `to_f64` for the |p_up−mid| comparison). **Live acceptance (`bot fair`, dev PC 2026-06-13 ~03:18 UTC, ~200 s, exit clean):** strike froze at the boundary as above; the model went Healthy after ~warmup and **published `p_up` next to the Up-token book mid throughout** (e.g. `p_up 0.379` vs `mid 0.345`), anchored mostly on basis-corrected Binance (164/192 snapshots) which the journal confirms lands within ~0.7 bps of Chainlink (basis ≈ −4.7 bps that window); the lone WARN was RTDS's documented subscribe-500 (`leger AddSubscriptions error`), self-healed. **Calibration finding (not a bug, what the journal is for):** mean |p_up − mid| ≈ 0.086 but with transient excursions to 0.163 (25/79 Healthy snapshots breached the 0.10 sanity band ≥ 3 s) — with the measured σ_1s ≈ 0.4 bps/√s, `p_up` is sharply sensitive to a few-dollar move near-the-money, and the basis-corrected fast anchor leads the book by ~2.7 s by design; the journal exists to quantify exactly this (σ calibration, markouts). Full post-hoc `verify` records need a window to resolve + the ~4 min metadata delay (the 200 s run ended before its window closed); the reconcile logic is covered by unit tests and the strike-source chain was verified by the plan-mode probes above. **4551:** config + feed-clob integration-test exes (crates untouched by this task) needed one remint-marker cycle to clear; all model/core-types/discovery/bot tests green.

- 2026-06-13 — **Composite model-health gate implemented** (`model::health` — new sans-IO `HealthMonitor`, 18 unit tests) + reworked `model::fair` + core-types health vocabulary + `bot fair` wiring. Completes the §8 defenses: the EWMA basis (`model::basis`) and the anchor correction were already done (prior entry); this task adds the **hysteretic three-tier gate** the engine will read. **Decisions:** (1) **`core_types::ModelHealth` renamed `Warming/Healthy/Unreliable` → `Ready/Degraded/Unreliable`** (operator-specified names) with `allows_quoting()` (Ready only), `requires_pull()` (Unreliable only), `is_degraded()`; derives `Ord` (worst-ascending: Ready < Degraded < Unreliable) so the machine compares tiers. **Warming folded into Unreliable** (operator left the call to me; pulling quotes at startup is a harmless no-op) with the cause carried in a new Copy `ModelHealthReason{Nominal,Warming,ChainlinkStale,NoAnchor,DivergenceHard,FastLeadLost,DivergenceSoft}` + `tier()` (the single reason→tier partition, test-pinned). `ModelSnapshot` gained `reason` next to `health`. (2) **DEGRADED = fast-lead-lost ∨ sustained soft divergence** (operator left this to me; §8 names adverse selection the core failure, so losing the fast Binance lead → anchor Chainlink-only → stop *new* quotes without force-cancelling; this **changes the prior "Chainlink-only = Healthy"** behaviour). UNRELIABLE = Chainlink stale ∨ no anchor ∨ sustained hard divergence ∨ not-warmed. (3) **`HealthMonitor` is sans-IO and per-asset**, living in `bot::fair::FairState` beside `vol`/`basis` — NOT in the per-window `FairValueEngine` (dropped on rollover), so hysteresis survives window rolls. Mirrors `timeutil::SkewMonitor`: inputs in with `now`, decisions out as values. (4) **Hysteresis = dual magnitude bounds + dwell timers, worsen-fast/ease-slow.** Thresholds (model-crate `HealthParams` consts, journal-calibratable): Chainlink stale ≥ 5000 ms, clears ≤ 3000 ms (dead zone holds the latch — anti-flap), **immediate** (the 5 s threshold is itself the enter debounce); divergence tiers SOFT 30 bps (clear 20) → DEGRADED, HARD 50 bps (clear 40) → UNRELIABLE, each gated by a **10 s confidence dwell** that filters the legitimate transient ~80 bps Binance-lead divergence (collapses in ~3 s ≪ 10 s — the magnitude bounds are *not* relied on for the lead); fast-lead-lost gated by a short **2 s dwell** (losing the §8 adverse-selection defence should degrade promptly, but debounce anchor flicker); every *recovery* to a better tier honours a 3 s **exit dwell** (`recovering_since`, reset on any relapse). Worsening to an unambiguous failure (stale/no-anchor) bypasses the exit dwell. Emits a `HealthTransition` only on a **gate-tier change** (latched, one per episode); same-tier reason refinement updates the snapshot but does not re-emit. (5) **Retired the instant-clear `divergence_since` latch** in `FairValueEngine` (and dropped `FairParams.divergence_bps`/`divergence_persist_ms`): the engine now only *measures* `divergence_bps`; the monitor owns persistence. `derive_health` deleted; `FairValue.health`/`reason` are **caller-stamped** (compute leaves placeholders; the driver runs the monitor on `FairValue::health_inputs()` and stamps the verdict — single producer of health). (6) **Latched `Event::ModelHealth(ModelHealthEvent{asset,health,reason,ts})`** added (operator-chosen), mirroring `FeedHealth`/`BookHealth` (feeds report, risk reacts): the engine/risk-manager match one event instead of inspecting every snapshot. ~16 B, so the widest variant stays `Model(ModelSnapshot)` ≤ 128 (size guard untouched). The driver buffers transitions in `FairState.pending_health` and publishes them from `on_heartbeat` (the single `model_tx` owner) via `try_send`, re-queuing on a full channel so a transition is never silently dropped. (7) **Config: zero new keys** — `health_params(feeds)` maps `feeds.rtds_stale_after_ms` → `chainlink_stale_ms` and derives `chainlink_fresh_ms = stale·3/5` (proportional, so a tighter `rtds_stale_after_ms` can never invert `fresh < stale` and refuse to construct); all other knobs are `HealthParams` consts (vol/basis precedent). `config_maps_into_model_params` extended. (8) **`bot fair` rendering**: model line now `[{health}/{reason} via {anchor}]`; a `MODEL HEALTH <asset> <from> -> <to> (<reason>)` line lands in the events pane on each transition; the ~1 Hz calibration `Snapshot` record gained `reason`. The `Snapshot` record was **boxed into `SnapshotRecord` behind `CalRecord::Snapshot(Box<…>)`** to satisfy `clippy::large_enum_variant` (the new field tipped it over) — internally-tagged serde still emits a flat `{"type":"snapshot",…}` (pinned by a new test). **Verification:** `cargo fmt`/`clippy --workspace --all-targets -D warnings` clean; per-package tests green — core-types 95, **model 68 (18 health: every transition + flapping/hysteresis, incl. the Decisions-Log-pinned `transient_lead_divergence_up_to_80bps_stays_ready`)**, bot 40+5. Live `bot fair` smoke (watch a natural Chainlink-stale or RTDS-500 force exactly one latched transition + the inverse on recovery, no flapping near the boundary) is the operator's to run interactively — left for the next live session, like prior `bot fair` acceptances. **4551:** the freshly-rebuilt `model` test exe was blocked once after `fmt`; deleting it + rebuild cleared it (these tests had already passed 68/68 pre-`fmt`).

- 2026-06-13 — **`venue-api` execution port + `venue-live` live adapter implemented** (venue-api: 3 unit tests; venue-live: 35 unit tests over a fake backend; bot: 41+5). The §4 paper/live seam + the §11 live-arming invariant, end to end. **SDK verified + pinned:** the official crate is `polymarket_client_sdk_v2` (UNDERSCORES — crates.io rejected the hyphenated form), pinned `=0.5.1` (`#[non_exhaustive]`-heavy; additive drift possible). `default-features=false, features=["clob"]` (NOT `ws` — the user-channel WebSocket is the next task). API confirmed by reading the crate source: type-state `clob::Client<auth::state::Authenticated<auth::Normal>>` built via `Client::new(host, Config::default())?.authentication_builder(&signer)[.funder(Address)].signature_type(SignatureType)[.credentials(Credentials::new(Uuid, String, String))].authenticate().await`; chain id rides on the signer (`with_chain_id(Some(137))`), not Config. Deposit-wallet sig type is **`SignatureType::Poly1271`** (=3); `OrderType` is UPPERCASE `GTC/GTD/FOK/FAK`. Order path is `client.limit_order()/market_order()` builder → `.build().await` (**fetches tick/neg-risk/fee over the network** — so signing is NOT offline) → `client.sign(&signer, SignableOrder)` → `post_order`/`post_orders`. Marketable amount via `Amount::usdc(Decimal)` (BUY $) / `Amount::shares(Decimal)` (SELL sh); market `price()` IS the worst-price limit. Cancels: `cancel_order(&str)`/`cancel_orders(&[&str])`/`cancel_all_orders()`/`cancel_market_orders(&CancelMarketOrderRequest)` → `CancelOrdersResponse { canceled: Vec<String>, not_canceled: HashMap<String,String> }`. Balances: `balance_allowance(BalanceAllowanceRequest)` → `{balance: Decimal, ..}`. Errors are an opaque `error::Error`; downcast to `error::Status { status_code, message }` for HTTP code + body. Request/response types live at `clob::types::request::*` / `clob::types::response::*` (the `clob::types` re-export is RFQ-only).
- 2026-06-13 — venue-api/venue-live design decisions: (1) **Public port is RPITIT** (`VenuePort`/`VenueEvents`, mirroring feed-util/discovery — no `async_trait`). The internal SDK seam `ClobPort` is ALSO RPITIT, with `LiveVenue<P: ClobPort>` generic over it — so all order-translation, error-mapping, batch-chunking, and reconcile logic is exercised OFFLINE through `FakeClobPort` (which records every `BuiltOrder`), and the SDK is isolated to `SdkClobPort`. This is how the acceptance "mocked transport proving request construction for every port method" is met without a network or `httpmock`-style dep (discovery `FakeApi` precedent). (2) **Sign vs submit are separated.** Since the SDK `build()` hits the network, fully-offline real signing is impossible; the offline tests assert request *construction* (our `BuiltOrder`), and `LiveVenue::dry_run` builds+signs against live params but never POSTs (the `dry_run` flag short-circuits `submit`). (3) **The §11 gate is the constructor.** `LiveVenue::connect` runs `check_arming` (boot gates → dashboard-armed → creds) FIRST and returns `NotArmed` before any SDK client is built — `with_backend` is `pub(crate)` so external code can only get a network-capable adapter through the gated path. `bot live` routes through `connect` and refuses at gate Boot/Dashboard (gate 3 hardwired false; live demonstrated via the new offline `bot venue-check`). (4) **GTD +60s floor lives in the adapter** (`convert::build`, `max(expires_at, now+60s+5s)`); the `core_types::TimeInForce::Gtd` doc was flipped to say the adapter owns it (was "caller's job"). `now` is a parameter (sans-IO convert). (5) **Interim event stream = REST open-orders poll** (`reconcile.rs` + `OrderStore::reconcile` pure diff honoring `can_transition_to`); emits `OrderUpdate`s for orders we placed (unknown venue orders need a token→window map → the WS-reconcile task). The real-time `/ws/user` push is the next task, behind the same `VenueEvent` mpsc channel. (6) **`[live].funder`** added to config (non-secret `0x…40-hex`, required when `live.enabled`); host/chain/sig-type are venue-live `LiveParams` code defaults mapped at the bot boundary (`bot::live::live_params`, vol_params precedent).
- 2026-06-13 — **Dependencies added (§3, all SDK-transitive / the official SDK + what it requires):** `polymarket_client_sdk_v2 = "=0.5.1"` (features `["clob"]`), `alloy-signer-local = "1.8"` (the SDK re-exports only the generic `LocalSigner`, so we name concrete `PrivateKeySigner` for the stored signer field — resolved 1.8.3), `chrono = "0.4"` (the order builder takes `DateTime<Utc>` for GTD; "time" stays the workspace clock crate). **TLS verified rustls-only — no openssl-sys, no native-tls** (`cargo tree -p venue-live -i openssl-sys` → not found). **But the SDK pulls `aws-lc-rs` as rustls's crypto provider** (alongside the feeds' ring), and **three rustls majors (0.23/0.26/0.27) + reqwest 0.13 coexist** with the workspace's reqwest 0.12 — heavier build, not a correctness issue offline. Two runtime items to verify when live first connects (live TLS is NOT exercised this task): (a) `aws-lc-sys` compiles C/assembly → the VPS needs `clang`+`cmake` (README updated); (b) the rustls process CryptoProvider must be installed for whichever rustls major the SDK's reqwest uses — the feeds install ring on 0.23; confirm the SDK path during the live-smoke run. **`balance_allowance.balance` denomination (USDC base-units ×10^6 vs human dollars) is unverified** — treated as dollars in `SdkClobPort::balances`, flagged to verify live before funding; positions left empty in the interim (engine tracks inventory from fills). **Deferred (documented in code + README):** the `/ws/user` real-time order/fill stream (next task) and the one-time on-chain ERC-20/CTF approvals via the gasless relayer (gasless/CTF task — a missing allowance surfaces as `Rejected(InsufficientFunds)`). **CLOB host:** defaulted to `clob.polymarket.com` (§7) though the v2 SDK examples use `clob-v2.polymarket.com` — operator overrides via `LiveParams` if needed; verify live. **4551:** the SDK build tripped Windows App Control on several dependency build-scripts (serde_json, crossbeam-utils, aws-lc-sys) and the bot test exe; `rm -rf target/debug/build/<crate>-*` + retry cleared each (per-hash remint).

- 2026-06-13 — **Order normalizer implemented** (`engine::normalize` — first `engine` module; pure/sans-IO; 24 unit tests) — the §7/§8 chokepoint every outgoing order intent passes through before a venue. `engine/Cargo.toml` gained direct deps `core-types`, `rust_decimal`, `thiserror` (all §3-allowlisted): **`model` does NOT re-export `core_types`**, so the engine couldn't name `MarketInfo`/`Price`/`TopOfBook` without the direct dep; no dev-deps (`dec!` via `rust_decimal`'s `macros` feature, tests assert on values). **Shape:** input `OrderDraft` (distinct from `NewOrder` — carries the *raw* target `price: Decimal` so a garbage price is a testable reject, not unrepresentable); `normalize(draft, market, book: Option<&TopOfBook>, params) -> Result<Normalized, NormalizeReject>` where `Normalized { order: NewOrder, adjustments: Adjustments }` and `Adjustments { price: PriceSnap, size_rounded_down, size_bumped_to_min }`. Step order: qty-kind gate → price snap → size/notional → post-only cross check (last, so the `WouldCross` payload is fully-sized) → assemble. Min-size floor = `max(5 shares, ceil_to_size_precision($1 / rounded_price), market.min_order_size)`, the $1 ceil recomputed against the **rounded** price; built on `Price::quantize` + `RoundDir::passive_for` + `Size::round_down_dp`. Division `$1 / rp` is panic-free (`rp` is a `Price`, strictly >0); every fallible op is `?`-mapped to a reject (no unwrap/expect/panic on runtime paths). **Decisions:** (1) **Size precision lives in `NormalizerParams { size_decimals: u32 }`** (default 0 = whole shares), mapped at the binary boundary like `VolParams`/`FairParams` — `MarketInfo` has **no** size-precision field (discovery reads only `order_min_size`/`order_price_min_tick_size`); the venue's true share precision must be verified against a live order ack (candidate to promote into `MarketInfo` later). (2) **Marketable price rounding is permissive** (slippage cap rounds *outward*: BUY→Up, SELL→Down via `passive_for(side.opposite())`), opposite to resting `passive_for(side)` — a worst-price limit must never be tightened into a no-fill; resting quotes keep passive (never-toward-crossing) rounding. (3) **Sizing never silently shrinks below intent** — a positive sub-minimum (incl. one that rounds to 0 at precision) is *bumped up* to the floor and flagged `size_bumped_to_min`; only a *literal* zero intent rejects (`ZeroSize`). A marketable-BUY notional `< $1` is **rejected** (`BelowMinNotional`), not bumped (raising spend is the risk manager's call). (4) **Engine-local `NormalizeReject`** (data-carrying pre-flight: PriceOutOfDomain/ZeroSize/QtyKindMismatch/BelowMinNotional/WouldCross) rather than reusing the flat post-hoc `venue_api::RejectReason`, with `as_reject_reason()` bridging at the journal/risk seam — mirrors `venue-live`'s `QtyKindMismatch` precedent; the qty-kind gate reuses `convert.rs`'s wording so a draft passing `normalize` can never trip `convert`'s `require_*`. (5) `Price::quantize` only ever returns `OutOfDomain`; the other `PriceError` arms are mapped defensively (no `unreachable!`/`panic!`). **Tests** (table-driven, `dec!`): all six required cases (0.10→10 sh, 0.50→5 sh, 0.001-grid near 0.99/0.01, tick-flip boundary 0.961-stays-fine, notional-just-below-$1→bump incl. price-rounds-down recompute, post-only would-cross) + edges (clamp-into-range flag, rounded-down, positive-rounds-to-zero→bump, literal-zero reject, qty-kind mismatch ×3, marketable <$1, permissive rounding ×2, out-of-domain, market-min binds, a construction-invariant sweep asserting `final≥5 ∧ final≥mkt-min ∧ final·price≥$1`, and the reject→venue bridge). Verified: `cargo fmt --check` + `cargo clippy -p engine --all-targets -D warnings` + `cargo test -p engine` (24/24) clean, and `cargo check --workspace` green (only the leaf `engine` crate changed). **Integration (DoD #6):** `Normalized.order: NewOrder` is exactly `venue-live::convert::build`'s input, type-checked end to end; **deferred** — wiring `normalize()` into the risk-manager → `VenuePort` call path and threading `NormalizerParams` from config at the binary boundary (next engine task).

- 2026-06-13 — **Authenticated `/ws/user` driver + canonical `OrderStore` implemented** (`venue-live`: `user_wire`, `user_ws`, rewritten `store`; 63 venue-live tests; bot 41+5). The §11/§9 order-feedback path: the real-time user WebSocket replaces the interim 2 s REST poll (`reconcile.rs`/`reconcile_loop` **deleted**, fold into the store + the WS task). **Docs verified (`market-data/websocket/user-channel`, re-read in full):** URL `wss://ws-subscriptions-clob.polymarket.com/ws/user`; subscribe `{"auth":{"apiKey","secret","passphrase"},"markets":[<condition_id>…],"type":"user"}` — by **condition ids**, not token ids; client sends text `PING` (≤10 s; we send 5 s), server replies `PONG`; `order` events carry `type` ∈ PLACEMENT/UPDATE/CANCELLATION + cumulative `size_matched`; `trade` events carry `status` ∈ MATCHED/MINED/CONFIRMED/RETRYING/FAILED, `taker_order_id`, and `maker_orders[]`. **Whether `markets:[]` streams ALL of the operator's orders is undocumented** — `LiveParams.subscribe_all_when_empty` (default true) governs the empty-set case; the design subscribes the explicit active set when one is supplied and the REST reconcile backstops either way (live-verify R1). **Architecture:** the WS task is **hand-rolled over feed-util's `Transport`/`Backoff` seam** (generic over `T: Transport` + `P: ClobPort`), following feed-clob's `run_connection` — NOT the SDK `ws` feature (deliberately off; no injectable transport/clock seam) and NOT feed-util's generic price-tick `run` driver. **New dep edge `venue-live → feed-util`** (inward — feed-util depends only on core-types; rustls-only, `cargo tree -p venue-live -i openssl-sys` empty); deps added `feed-util` + `serde` + `serde_json` (+ dev `feed-util/fake`, all §3-allowlisted). The store and wire parser stay **sans-IO** (clock injected; effects returned as values), so the whole machine is driven offline by `feed_util::fake` + `FakeClobPort` under paused time. **Canonical `OrderStore` idempotency model** (the heart, operator decisions folded in): (1) **state + cumulative `filled_size`** driven by `order` events + REST reconcile, combined **monotone-max** with a **terminal latch** and `OrderState::can_transition_to` — a stale/duplicate cumulative is a no-op, a late *order* event after a terminal is dropped (an illegal transition is dropped + warned). (2) **discrete `Fill`s** driven by `trade` events, deduped by trade id (FIFO-bounded `seen_trades`, cap 50 000, for 24/7); `Retrying`/`Failed` emit nothing **and do not consume the id** (a retry may still settle); role = taker iff `taker_order_id` is ours, maker for each of our `maker_orders` legs → `Liquidity` + fee (`Dollars::ZERO` maker / `taker_fee` taker); fills use the tracked order's own limit price (exact for our post-only makers; a tracked-order *taker* never occurs since marketable orders are not tracked — its exec-price fidelity is a noted follow-up). Emitted fill quantity is clamped to `original_size` so a stray status can't over-emit. (3) **Missed fills on reconnect → synthetic maker `Fill` + `OrderUpdate` correction** (operator choice): `reconcile` emits a synthetic `Fill{trade_id:None, Maker, fee 0}` for `size_matched − emitted` (exact for our resting orders) and tracks `synthetic_covered` so a later real `trade` for those shares is suppressed (no double-count); `LiveParams.emit_synthetic_fills` (default true) toggles to corrections-only. A trade that arrives *before* the reconcile leaves `synthetic_covered = 0` and the reconcile delta 0 — covered both ways (tested). **`StoreEffect{Order,Fill}` is `pub(crate)`** (the wire types it consumes live in a private module; keeping it crate-internal avoids leaking them — the driver maps each to `VenueEvent::{Order,Fill}`). **Attribution seam shipped:** `WindowIndex` (token → `WindowCtx{window,outcome,fee_rate,tick}`) + enriched `RawOpenOrder` (now also `token_id/side/price/condition_id`, mapped in `sdk::open_orders`: SDK `B256` market → `0x`+64-hex condition id, `U256` asset → bare-decimal token id — **pinned by an offline `sdk` unit test**, re-verify live R2/R3). **Orphan adoption is implemented and unit-tested** (a prior-process survivor in the REST poll is adopted when the index resolves it, price via `Price::on_grid` fail-closed) but **inert in production until the orchestrator wires a `WindowIndex` via `LiveVenue::set_window_index`** (deferred per the operator's "best-engineering" call — this honours "defer the orchestrator wiring + live tick path" while shipping a tested, complete seam). `fee_rate` for tracked orders is seeded from the index else `LiveParams.default_taker_fee_rate` (default 0.07, §7 config-default — never hardcoded in fee logic). **Markets** flow via `LiveVenue::set_markets` → `watch::Receiver<Vec<ConditionId>>`; a change reconnects to resubscribe (reconnect → on-connect reconcile, so no fills lost). **Creds:** `UserWsCreds::from_secrets` (requires operator-supplied L2 creds — the §11 gate guarantees them; redacting `Debug`, never serialized). **`connect` now spawns `user_ws::run`** (gate-first unchanged; `dry_run` spawns nothing); `LiveVenue` holds `markets_tx`/`shutdown_tx` (a dropped `shutdown_tx` also stops the task). **Reconcile cadence:** full REST reconcile on **every (re)connect** (before streaming, so a gap-fill is ordered ahead of new events) + a `safety_reconcile_interval` (30 s) tick while connected. **`LiveParams` gained** `user_ws_url`, `ws_ping_interval` (5 s), `ws_dead_after` (3× ping), `ws_connect_timeout` (10 s), `ws_backoff` ({250 ms,10 s,2.0}), `safety_reconcile_interval` (30 s), `subscribe_all_when_empty` (true), `emit_synthetic_fills` (true), `default_taker_fee_rate` (0.07) — all code defaults mapped at `bot::live::live_params` via `..default()` (zero new figment keys); **`LiveParams` dropped `Eq`** (the backoff multiplier is `f64`). **Fixtures** (`crates/venue-live/tests/fixtures/`, provenance README): documented `order`/`trade` shapes synthesized from the docs field list + SDK 0.5.1 `clob::ws` response types (marked, replace with a live capture — `feed-clob` tick_size_change precedent); consumed via `include_str!` by the `user_wire` parse tests and a `store` full-lifecycle test (placement → update → taker fill → maker fill → FAILED no-op → cancellation). **Test deviation from the approved plan:** the driver and fixture tests are in-crate unit tests (paused-time `FakeTransport`+`FakeClobPort`; `include_str!` fixtures) rather than external `tests/*.rs` files — keeps `StoreEffect`/wire types crate-internal. The three acceptance scenarios are the headline driver tests: `connect_subscribes_then_reconciles_then_streams`, `reconnect_recovers_missed_fill_then_dedups_the_real_trade`, `duplicate_trade_over_the_wire_emits_one_fill` (+ ping cadence, markets-change resubscribe). **Verification:** `cargo fmt --all --check` + `cargo clippy --workspace --all-targets -D warnings` clean; `cargo test -p venue-live` 63/63, `cargo test -p bot` 41+5 green. **Live-verify items (offline tests pass regardless; check on first armed connect):** R1 markets-empty semantics; R2/R3 the SDK order-row field names/id formats + maker/taker size/price provenance; R8 the rustls process provider with the SDK's `aws-lc-rs` and feed-util's `ring` coexisting (feed-util installs `ring` for the WS path). **Deferred follow-ups:** the scheduler→`set_window_index`/`set_markets` wiring; pruning terminal orders from the map for very long sessions (a `forget_window` hook; `seen_trades` is already bounded). **4551:** none this task (per-package `cargo check`/`clippy`/`test -p venue-live` ran clean without remint).

- 2026-06-13 — **`venue-paper` fill simulator implemented** (`params`/`latency`/`book`/`wallet`/`engine`/`driver`; 47 tests) + `bot paper-sim` smoke + `bot::paper::paper_params` boundary mapping. The §9 paper venue: real Polymarket book/trade data, paper money, **never more optimistic than reality**. Mirrors `venue-live`'s shape — a sans-IO `MatchEngine` whose clock-injected mutators return `Vec<PaperEffect{Order,Fill}>`, wrapped by an `Arc<Mutex<…>>` `PaperVenue` (std Mutex, never held across `await`; lock→mutate→collect→unlock→`event_tx.send().await`) plus one spawned **timer task** that drives simulated-latency deadlines via `on_clock`, woken by a `Notify`. Market data is pushed in through `PaperVenue::on_bus_event(&Event)`; the order/fill stream is the same `VenueEvent` channel as live (paper & live are indistinguishable to the engine). **No arming gate** (paper is always constructible). **Dependency decision (§2.8, diverges from §4):** dropped the listed `feed-clob` dep — everything paper consumes (`Event`, `BookSnapshot`, `BookLevel`, `LastTrade`, `TopOfBook`, `MarketInfo`, `taker_fee`) lives in `core-types` now (feed-clob's `BookState` is private), so `venue-paper` depends on **`core-types`** instead for a leaner rebuild; arrows still point inward. Deps: core-types, venue-api, tokio (rt/sync/time/macros), tracing, rust_decimal, thiserror (no timeutil — the engine is clock-free; the binary passes `wall_now` as `now_fn`). **Conservative fill rules (the heart):** (1) **placement latency** — `place()` registers `PendingNew` with `activate_at = now + sample(placement)`, returns `Ok(Accepted{PendingNew})` and publishes nothing; the real outcome (Open / Rejected / marketable fills) is emitted asynchronously once the latency elapses, faithfully modeling the wire round-trip. This **differs from live's synchronous `place()` reject** — only a *structurally-invalid* order (qty kind ⊥ TIF/side, which the normalizer forbids upstream) rejects synchronously; the post-only cross is judged at activation against the activation-time book, exactly where live's venue judges it. (2) **queue behind ALL displayed size** captured at activation (`SimBook::displayed_at`), only ever drains. (3) **resting fills** only from prints whose aggressor is the OPPOSITE side, at-or-through our price; the print size is a **finite shared budget** processed in realistic priority (best price, then `activate_at`/`seq`), each order draining its own queue from that budget before filling — no double-count; Maker, fee 0, at our limit price. Matching is pure price-and-queue — **never gated on whether the book snapshot already reflected the trade** (prints can precede their snapshot, §7). (4) **cancel race** — `cancel()` sets `cancel_effect_at = now + sample(cancel)` and returns the live-style `canceled:[id]` ack but keeps the order working+fillable and emits **no `PendingCancel`** (mirrors live's external Open→Canceled sequence); prints in `[request, effect)` still fill the stale quote (we lose the race), then `on_clock` cancels the remainder — the §9 adverse case, unit-pinned. (5) **marketable (FOK/FAK)** walk real displayed depth respecting the worst-price limit (BUY by dollar budget against asks, SELL by share count against bids), Taker fills at the level price with `taker_fee(per-market rate)`, and **deplete the local book copy** so two simultaneous paper takers can't double-eat depth (overwritten by the next snapshot). FOK all-or-nothing (`Rejected{FokUnfilled}`, no partial); FAK partial→`Canceled`, zero→`Rejected{FakNoMatch}`. (6) post-only-would-cross at activation→`Rejected{CrossedBook}`. (7) window `Closed`/`Resolved` terminalizes still-working orders (`Canceled`); GTD expiry→`Expired` (floored to `now+60s`, §7). Order ids `paper-{n}`; synthetic paper trade ids `paper-t{n}`. Time discipline: `now` (local) drives all deadlines, print **price** drives match eligibility, `Fill.ts_venue` = print ts; no wall/exchange-time mixing. **Latency** = uniform `[mean, mean+jitter]`, one-sided-up (`jitter=0` ⇒ exactly mean), via an inlined ~15-line xorshift64* seeded from `PaperParams.rng_seed` (feed-util's is `pub(crate)`, not reusable). **Wallet** = minimal fill-driven ledger (collateral cash-flow + signed net positions + a maker-rebate-*estimate* accumulator) for `balances()`; `set_capital` seam present. **Scope deferred to the paper-ledger task (seams in place, per operator's "real data, paper money, merge simulated"):** settlement on the real `market_resolved`, instant simulated pair-merge, the daily maker-rebate credit cycle, open-order collateral reservation (`available == total` in v1), negative-net (short/merge) reporting. **`PaperParams`** is plain (no `config` dep); `bot::paper::paper_params` maps `config.paper` at the boundary (vol_params/live_params precedent); zero new config keys (reuses the existing `[paper]` section). **`bot paper-sim <series>`** (default BTC-5m) wires scheduler+feed-clob (ladder's wiring) → `PaperVenue` → a trivial two-sided post-only quoter that re-quotes at the live touch every 5 s, rendering quotes/fills/wallet in place — the §12.6 demonstration (operator-run live, like `bot fair`/`ladder`); the conservative rules are pinned by the unit tests, incl. the headline `a_print_fills_the_stale_quote_before_the_cancel_lands` and `simultaneous_takers_cannot_double_eat_displayed_depth`. The driver integration test (`#[tokio::test(start_paused=true)]`) spawns `PaperVenue`, feeds bus events, places via the port, advances virtual time, and asserts the `VenueEvent` Open/Fill stream + balances. **Verification:** `cargo fmt --all --check` clean; `cargo clippy -p venue-paper -p bot --all-targets -D warnings` clean; `cargo check --workspace` green; `cargo test -p venue-paper` **47/47**; `cargo test -p bot` **42 unit + 5 integration green** (the 5 spawned-binary integration tests passed once on the first reminted build). **4551:** heavy this session — the rebuilt `bot` test exe AND `bot.exe` itself were hash-sticky blocked across many retries/deletes/comment-reminted edits; one remint cleared the 42 unit tests but the 5 integration tests (which spawn `bot.exe`) stayed blocked because `bot.exe`'s spawn was blocked even when run directly (warm exit 126). `cargo test -p venue-paper` was unaffected throughout; the integration logic is identical to the earlier clean pass — re-run `cargo test -p bot` when App Control settles (irrelevant on the Linux VPS).

- 2026-06-14 — **Paper ledger implemented** (`venue-paper`: `wallet`/`engine`/`driver` extended; 59 tests, up from 47) — settlement, instant pair-merge, the daily maker-rebate credit cycle, runtime capital set/adjust, and separate fee/rebate income lines, all conservation-tested. The wallet stays a cash + signed-per-token-shares ledger; **cost-basis is intentionally NOT tracked** (operator-confirmed scope is cash + shares; settlement marks every share at $1/$0 so realized edge falls out of cash — per-side cost for the §8 pair-discipline analytics stays in `core_types::SideInventory`). **New `PaperWallet` state:** `fees_paid` (cumulative taker fees, incremented one line in `apply_fill` — zero on maker fills), `rebate_credited` (the daily-credited income line), `capital_injected` (start + signed runtime deltas — the conservation anchor). **Ops:** `settle_window(up,down,winner)` pays the winning token's signed net × $1 and zeroes both positions — **signed arithmetic handles shorts** (a net-short winner debits cash, pinned by `settles_signed_short`); `merge(up,down,requested)` removes `min(matched, requested)` pairs and credits $1/pair (only when both sides net long); `credit_rebate(min)` moves the whole accrued balance to cash once it reaches the $1 floor, else carries over; `set_capital` records the implied delta as injected capital so the identity still closes; `adjust_capital(delta)` for the +/- seam. **Engine wiring:** `on_window` settles **once** on `Resolved{outcome}` (idempotent via a `settled: HashSet<WindowId>`; `Closed` never settles — outcome unknown) reading tokens straight off the event metadata; `merge(window, requested, now)` looks up the window's `TokenPair` in `self.markets` (unknown window ⇒ `VenueError::VenueInternal`), emits no `PaperEffect`; the **daily rebate cycle** is an engine-global deadline `rebate_next_credit_at`, armed lazily on the first maker-rebate accrual in `on_trade` (`new` has no clock), folded into BOTH `next_deadline` (timer sleeps to it) and `on_clock` (a `while`-loop `run_rebate_cycle` catches up every elapsed 24h period; sub-$1 periods carry) — **so the timer task is unchanged**. `REBATE_PERIOD = 24h` and the $1 floor are engine consts (no new config keys; the existing `[paper]` section + `paper_params` boundary mapping are untouched). **Driver/port:** `PaperVenue` gains sync `set_capital`/`adjust_capital`/`ledger_snapshot` (the venue-live `set_markets` lock-and-mutate idiom — no `await`, no `VenueEvent`, no timer nudge) and async `merge` (via `apply` for now/notify symmetry though it schedules nothing); `balances()` and `VenueEvent{Order,Fill}` unchanged. **Decision (operator):** `merge` is a concrete `PaperVenue` method, NOT on `VenuePort` — promoting it to a venue-agnostic port + the live CTF on-chain merge is a documented follow-up; the engine quoting layer that will *request* merges isn't built yet. **Decision (operator):** capital-adjust journaling = `ControlEvent::PaperCapitalSet` emitted **on the bus by the binary** (venue-paper holds no bus sender) + a structured `tracing` line in the engine (`target:"paper-ledger"`); durable persistence is deferred to the `journal` crate (still an empty stub). **Mechanism:** settlement/merge/rebate-credit are pure wallet mutations surfaced via the `balances()` poll + a new richer `PaperLedgerSnapshot` (cash, signed positions, fees_paid, rebate_accrued, rebate_credited) + `tracing` — `VenueEvent` deliberately stays `{Order,Fill}`-only. **`PaperLedgerSnapshot` lives in `wallet.rs`** (deviation from the plan, which placed it in `driver.rs`): the engine's `ledger_snapshot()` returns it, so housing it in the wallet avoids an engine→driver module cycle; re-exported via `lib.rs`. **Conservation tests (the headline, no `proptest` dep):** `wallet::conservation` runs 8 seeds × 400 random ops (fills buy/sell × maker/taker, merges, settlements, capital ±, rebate credits) over a hand-rolled xorshift64* (the `latency.rs` idiom), driving an **independent shadow oracle** built only from operation inputs + spec formulas; after every step it asserts the wallet equals the oracle on every accumulator (cash, per-token shares, fees, rebate accrued/credited, capital injected) by exact `Decimal` equality, and the closing identity collapses to `wallet.collateral == oracle.cash` once all positions settle — anti-circular because the ledger number is only ever checked against the oracle, never recomputed via the ledger. Plus narrow exact unit tests (settlement $1/winning share incl. the signed-short case; merge $1/pair + cap-at-requested + skip-one-sided; rebate $1-min carryover; taker-only fee accumulation; capital injection tracking) and 3 scripted engine tests (resolved-settles-and-idempotent; merge-converts-pairs + unknown-window-errors; rebate-credits-after-a-day-via-the-deadline). **`bot paper-sim` demo (§12.6):** income line from `ledger_snapshot` (fees + rebate accrued/credited, ledger-authoritative); the quoter now also bids the Down token so matched pairs form, then `try_merge` recycles them (`MERGE n pairs` line, `merges` counter); a `SETTLED slug -> outcome collateral X` line read from the post-settle balance on the bus `Resolved`; a one-shot `adjust_capital(+$1000)` ~30 s in that logs, emits `ControlEvent::PaperCapitalSet` on the bus (the binary retains a `control_tx` clone), and prints a `CAPITAL` line; summary gains rebate-credited + merges. **No new dependencies, no new config keys.** **Deferred (seams in place):** open-order collateral reservation (`available == total`), `merge` port-promotion + live CTF merge, durable journaling, optional config exposure of the rebate period/floor. **Verification:** `cargo fmt --all --check` + `cargo clippy --workspace --all-targets -D warnings` clean; `cargo test -p venue-paper` **59/59** (incl. the conservation property suite); `cargo test -p bot` **42 unit + 5 integration green** on retry. **4551:** one integration test (`live_subcommand_refuses_when_disarmed`, spawns `bot.exe`) tripped 4551 on the first run and passed on immediate retry — the documented per-execution scanner race, not a code issue.

- 2026-06-14 — **Journal recorder + replay reader implemented** (`journal`: `record`/`recorder`/`replay`, 9 unit tests) + **`bot record` subcommand** (+ `justfile record`, README §) + **venue-parity golden tests** (`crates/bot/tests/venue_parity.rs`, 13 tests) + **engine architecture lint** (`crates/engine/tests/architecture.rs`, 2 tests). Fills the §4 journal stub with the file-based recorder the operator asked for (sqlite index still a documented follow-up; the raw segments are the source of truth). **Dependencies added to `journal`** (all §3-allowlisted except flate2): serde, serde_json, tokio (`sync` only — the writer is a std thread, not a tokio task), tracing, thiserror, time (segment stamp), and **flate2** `default-features=false, features=["rust_backend"]` — the **one allowlist deviation, operator-mandated** (the gzip-segments decision): the pure-Rust miniz_oxide backend means **no C/cmake build** (sidesteps the aws-lc-style 4551 pain) and no openssl; gzip is `gunzip`/`zcat`-replayable. §3 allowlist prose updated. `rust_decimal` is a journal **dev-dep** (`dec!` in tests). **`JournalRecord`** (`record.rs`) is the serde projection of the bus `Event` (which is deliberately NOT serde): an internally-tagged enum (`#[serde(tag="type", rename_all="snake_case")]`, the `CalRecord` precedent) mirroring `Event` 1:1, holding inner payloads by value — wide variants (`Book`/`Window`-`MarketInfo`/`OrderUpdate`/`Fill`/`Inventory`) **boxed** so the enum stays small (boxing is transparent to serde). `from_event` derefs the `Arc`s; `to_event` re-wraps them — **lossless round-trip** verified for every variant, and the `from_event` match is **exhaustive** (no `_` arm; `Event` is intentionally not `#[non_exhaustive]`, so a new bus variant forces this to update). Internally-tagged enums round-trip the four enum-wrapping variants (FeedHealth/BookHealth/Risk/Control) fine — confirmed live, not just assumed. One `RecordEnvelope { seq, ts_local_ms, rec }` per JSONL line (nested `rec`, not flattened — avoids the serde flatten×internal-tag edge case). **`Recorder`** (`recorder.rs`) is off-hot-path: `record(&Event)` clones the (Arc-cheap) event and `try_send`s onto a **bounded** tokio channel (default 16384), **dropping + counting** on a full channel — never blocking the bus, never unbounded-growing over days (the load-bearing deviation from the per-record-flushed `spawn_jsonl_writer`); a dedicated **OS thread** does the `from_event` projection + JSON serialize + write into a `GzEncoder<BufWriter<File>>`, **buffered with periodic + catch-up flush** (per-record fsync at ~1500 msg/s would defeat the buffer), **rotating on 128 MiB-uncompressed OR 1 h** into `journal-{YYYYMMDD-HHMMSS}-{NNNNN}.jsonl.gz` (the `{NNNNN}` suffix keeps lexical == chrono order within a second). `finish()` drains, flushes, finalizes the gzip trailers, returns `RecorderStats{records,dropped,segments}`. **`ReplayReader`** (`replay.rs`) `read_dir`-globs + sorts the segments (no `glob` crate), decodes with `MultiGzDecoder`, yields `RecordEnvelope` per line; **`.events()`** maps back to bus `Event`s. Robust by design: an unreadable segment is skipped and a crash-truncated trailing segment (corrupt gzip tail, or a final newline-less line) yields what decoded then stops — pinned by a truncation test. **`bot record`** (`record.rs`) clones the `fair`/`ladder` wiring **minus the model**: scheduler (all `config.engine.enabled_series()`, or `--series`) + feed-clob + feed-rtds + feed-binance on one bus; the loop hands every event to the recorder and forwards `Event::Window` to feed-clob's `window_tx`; ctrl-c drains then `finish()`+summary. `--out-dir` flag (default `data/journal/`, gated to `record`). **Venue-parity golden tests:** the §2.5/§9 proof the engine can't tell paper from live apart at the `VenuePort`/`VenueEvent` surface. **Scope decision (operator: "whatever is good"):** drive **venue-paper only** against golden normalized `VenueEvent` sequences that encode the same contract venue-live's ~63 in-crate `FakeClobPort` tests already pin — **NOT** a live/paper side-by-side diff (which would need a feature-gated `pub` test seam piercing the §11 safety boundary of `LiveVenue::connect`, for marginal gain; a runtime diff, if ever wanted, belongs in venue-live's own `tests/` under its `fake` feature — documented follow-up). A local `normalize(&VenueEvent)` drops venue-specific ids/timestamps/exact-fee, keeping {kind, alias(=client_id), state, side, price, size, liquidity, reject-class, fee-zero}; 13 `#[tokio::test(start_paused=true)]` scenarios (A resting→partial-maker-fill, B post-only-cross, C1 FOK-full, C2 FOK-unfillable, D FAK-partial→cancel, D2 FAK-no-match, E cancel-race, F cancel_all, G cancel-unknown, H qty-kind-mismatch, I window-close, J GTD-expiry, + a record→replay dogfood) each cross-reference the live test that pins the same behavior. **Contract fact pinned (the most likely golden bug, per the plan review):** paper's cross/FOK/FAK rejects are **asynchronous** `VenueEvent::Order{Rejected}` from the timer task; the **only** synchronous `place()`→`VenueError` is the structural qty-kind mismatch (scenario H). Collection relies on tokio paused-time **auto-advance** (recv with a generous timeout jumps the virtual clock to the next engine deadline) — no hand-rolled `advance`; a short-timeout `assert_drained` confirms no extra event (the daily rebate deadline emits no `VenueEvent`, so it can't spuriously trip it). The dogfood scenario records a session through `journal::Recorder` and replays it via `ReplayReader::events()` into `PaperVenue::on_bus_event`, driving a real fill — the same path `bot record` captures. **Engine architecture lint** (`engine/tests/architecture.rs`): a section-aware scan of engine's own `Cargo.toml` (via `include_str!`; no `cargo_metadata` — not on the allowlist) asserts `venue-api` is declared and `venue-live`/`venue-paper` are **not** (§4 inward-arrows) — the compiler only complains on *use*, this guards the *declaration*. Lives with the crate it guards (4551-safe per-package). bot gained a `[dev-dependencies] tokio` with `test-util` (the paused clock); no new lib deps, zero new config keys. **Verification:** `cargo fmt --all --check` + `cargo clippy --workspace --all-targets -D warnings` clean; `cargo test -p journal` **9/9**, `cargo test -p bot --test venue_parity` **13/13**, `cargo test -p engine --test architecture` **2/2**, `cargo test -p bot --bin bot` **43/43** (incl. the new `parses_record_flags`). Integration test files use the codebase's `#![allow(clippy::expect_used, unwrap_used, panic)]` idiom (helpers aren't `#[test]` fns, so the clippy.toml exemption doesn't reach them). **4551:** the freshly-linked `journal` test exe was hash-sticky blocked twice; deleting `target/debug/deps/journal-*.exe` + rebuild cleared it (the documented per-hash remedy). The full `bot` integration suite (`check_config` spawning `bot.exe`) is the operator's to re-run when App Control settles, like prior tasks; the live `bot record` smoke (capture all six series, ctrl-c flush, `gunzip`-decode + replay round-trip) is left for the next live session like prior `fair`/`ladder` acceptances.

- 2026-06-14 — **Engine inventory management implemented** (`engine::inventory` — new module, 18 unit tests) + new `core_types::SettlementSummary` + `Event::Settlement` + journal arm + `merge_min_pairs` config knob + `bot/tests/inventory_replay.rs` rebuild-from-journal integration test. The §8 per-window pair-accounting brain: folds fills into a per-window book and answers the quoting/risk/merge policy questions. **Architecture (sans-IO, event-time-driven):** `WindowInventory` holds `{window, up/down: SideInventory, cash_flow, fees_paid, merged_pairs, settled: Option<Outcome>}` and **carries no market metadata** — a `Fill` already has its decoded `outcome` and charged `fee`, and settlement reads the winner + close time off the resolved-window event, so a fill arriving before its `Window` event folds identically on live and replay (this dropped the planned `tokens`/`fee_rate` fields, killing a stale-copy divergence risk). `InventoryManager{books: HashMap<WindowId,_>, settled: HashSet}` folds `Event::Fill` (→ one `InventoryEffect::Snapshot` stamped `fill.ts_venue`) and `Event::Window{Resolved}` (→ at most one `InventoryEffect::Settled` stamped `market.close_time`, idempotent via the settled set; an untraded window emits no summary); it **never iterates `books`** and uses **no wall clock**, so `InventoryManager::rebuild(events)` reproduces state AND the effect stream bit-for-bit (the §3/§9 restart requirement, proven end-to-end by `inventory_replay.rs` through the real `Recorder`→`ReplayReader`). **Money model (conservation-tested):** `cash_flow` is the single signed accumulator — BUY `−=p·sz −fee`, SELL `+=p·sold −fee` reducing shares at the **moving-average cost basis captured before mutating** (avg preserved across a partial sell; long-only, an over-sell clamps to held shares — defensive, silent, no `debug_assert`/panic so §12 holds and the clamp stays testable), MERGE `+=n·$1`; `realized_pnl = cash_flow + winning_side.shares·$1`. An 8-seed × 300-op xorshift64* shadow-oracle (the wallet idiom) recomputes cash/fees/merged/shares/realized from raw inputs and asserts exact-`Decimal` equality at every step + at close. **Pure §8 queries on `WindowInventory`:** `pair_cost_after_add`/`authorizes_passive_add` (post-trade `avg_up+avg_down ≤ threshold`; a one-sided add is authorized — pair discipline governs pairs, the caps govern a lone side), `excess_constraint(soft,hard) → Normal|SoftCapped|HardCapped{excess_side}`, `merge_intent(min) → Option<MergeIntent>` (at-or-above the configured size, `None` once settled), `worst_case_exceeds(limit)`. **Merge scoped advisory (operator left the call to me):** `merge_intent` *requests* and `apply_merge` reflects a confirmed merge (reduce both sides at avg, `+$1`/pair, preserve avg, cap at matched — fully unit-tested), but `on_event` **never drives a merge** — merge *execution* (`PaperVenue::merge`/live CTF → engine) stays deferred per CLAUDE.md, so no merge mutates runtime state yet and rebuild from Fill+Window is exact; documented follow-up: wiring execution must add a journaled merge-confirmation event (consumed by `on_event`) to keep rebuild exact. **`SettlementSummary` (new core-types type, raw numbers for analytics):** `{window, outcome, up, down, matched_pairs, pair_cost, excess, merged_pairs, fees_paid, realized_pnl, ts}` via a single `::close` that derives the matched/pair-cost/excess fields through `InventorySnapshot::derive` (no drift); analytics owns the §10 locked-pair-vs-inventory split (a pure function of these fields). **New `Event::Settlement(Arc<SettlementSummary>)`** (size guard untouched — Arc is 8 B) + `JournalRecord::Settlement(Box<…>)` + `from_event`/`to_event` arms + a `sample_events` entry (the only exhaustive `Event` match in the tree is journal's, which forced both arms). **Config:** added `merge_min_pairs: Size` to `EngineParams`/`EngineParamsPatch`/`apply`/`default.toml` (default **25** pairs — with touch 10 + 3×10 ladder a window forms tens of pairs; recycle mid-window without churn; orthogonal to the excess caps so no cross-field rule) + a `validate_into` rule (`> 0`); the existing `patch_covers_every_params_field` anti-drift test auto-pins the new field. `InventoryParams{pair_cost_threshold, soft/hard_cap_excess, merge_min_pairs, max_worst_case_loss}` + `Default` is the engine-local bundle; **the `config::EngineParams → InventoryParams` boundary map at the `bot` binary is deferred** to the engine-bus-wiring task, exactly as `NormalizerParams` deferred its own mapping (documented in the module doc so it reads as a choice). No new dependencies; engine stays tracing-free/pure like the normalizer; `bot` already depended on `engine`. **Verification:** `cargo fmt --all --check` + `cargo clippy --workspace --all-targets -D warnings` clean; per-package green — core-types **97**, journal **9**, config **67+2**, engine **43 lib (incl. the conservation oracle) + 2 architecture**, bot **43 bin + 1 inventory_replay + 13 venue_parity**; `cargo check --workspace` clean. **4551:** the `config` test exe was hash-sticky blocked on three consecutive runs (it had passed 67/67 earlier the same session); a trivial doc-comment remint on `config/sections/engine.rs` + exe delete cleared it. Deferred follow-ups: the `bot` boundary map + its anti-drift test; merge execution + its journaled confirmation event; analytics consuming `Event::Settlement`; `forget_window` pruning of long-settled books.

- 2026-06-14 — **Quoting calculator implemented** (`engine::quoting` — new module, the §8 strategy brain; 24 hand-computed table-driven unit tests) + three shared `*_sides` free fns extracted into `engine::inventory`. The pure `calculate_quotes(model, inv, fees, params, tau_secs, tick) -> QuoteDecision` turns a `ModelSnapshot` + `InventorySnapshot` into the complete desired set of resting maker quotes, or `NoQuote(reason)`. **Sans-IO/pure** (no clock, no log, no alloc beyond the returned `Vec`s, no unwrap/expect/panic on runtime paths — every fallible step maps to a skipped level or a typed reason), the normalize/inventory discipline. No new dependencies; engine stays `model`/`venue-api`/`core-types`-only. **Market model (load-bearing, operator left it to me, confirmed against `bot paper-sim`'s requote):** Polymarket has no shorting, so two-sided MM = **two BUY ladders** (the §7 mirror BUY Up @ p ≡ SELL Down @ 1−p): Up-buys descending from `center − half_spread`, Down-buys descending from `(1 − center) − half_spread`, both `Side::Buy` post-only `Gtc`. `down_fair = 1 − center` (NOT independently re-skewed) so the two fairs sum to 1 and the pair fair is `1 − 2·hs`, keeping pair-cost discipline predictable. **Skew (verified direction):** `center = p_up − γ·norm_imbalance`, `norm_imbalance = clamp(excess / hard_cap, −1, 1)`; long Up lowers center (bid Up lower) and raises `1−center` (bid Down higher) → rebalances. **Decisions (operator deferred all three judgment calls to me):** (1) **latency premium = `σ_1s·√(cancel_rtt_secs)`** — the expected adverse underlying move over the cancel round-trip we can't pull within; vol-scaled, unit-consistent with the vol term, trivially cheap; `cancel_rtt_secs` a `QuoteParams` knob (default 0.2 s ≈ measured ~180 ms REST p50). So `half_spread = max(min_edge, σ·(k1·√hold·spike_mult + √rtt))`. (2) **explicit vol-spike multiplier** on top of the inherent σ-proportional widening: `σ_1s ≥ vol_spike_threshold_1s ⇒ ×vol_spike_mult` on the vol term, flagged `vol_spike` (a single snapshot has no history, so the spike is the absolute σ level; defaults 0.001 / 1.5×) — makes the §8 "widen on vol spikes" rule a distinct, hand-testable behavior. (3) **engine-local `QuoteParams` + `Default`; config→`QuoteParams` mapping DEFERRED** to the engine-bus-wiring task (the `NormalizerParams`/`InventoryParams`/`VolParams`/`HealthParams` precedent) — new knobs absent from `config::EngineParams` (cancel_rtt, atm_band, vol_spike threshold/mult) stay crate-local; **the config crate was not touched.** **Gate precedence (early-exit, the §8 rules):** G1 model health (`allows_quoting()` — Ready only, so Degraded *and* Unreliable pull) → G2 active window + matches inventory window → G3 expiry (τ≤0/NaN) → G4 final-`no_passive_final_secs` cancel-all (whole `NoQuote`) → G5 usable p_up∈(0,1)/σ>0 → then S1 imbalance, S2 skewed+clamped center, S3 half-spread, **S4 excess caps:** `HardCapped ⇒ NoQuote(InventoryBlocked)` (the `ExcessConstraint` doc says "stop passive making **entirely**"; working the excess off is a taker concern, out of scope), `SoftCapped{side}`/worst-case-breach ⇒ silence the **excess side** only (deficit quotes on, recorded `SoftCapExcessSide`/`WorstCaseExcessSide`); **per eligible side** S5 build ladder → S6 final-`no_atm_final_secs` ATM gate (per-level, on the **quote price**, `|price−0.5| ≤ atm_band`) → S7 pair-cost authorization (`authorizes_passive_add_sides`; a one-sided add with the opposite empty is authorized) → S8 within-side grid-collision dedupe. A one-sided survivor is `Quote`, never `NoQuote`; only an empty level set ⇒ `NoSurvivingLevels`. Diagnostics carried in `QuoteSet` (`center`/`half_spread` as 6-dp `Decimal`, `p_up`, `normalized_imbalance`, `vol_spike`, a `suppressed: Vec<Suppressed{outcome, level, reason}>`). The calculator carries **no market metadata** (emits `Outcome`; the caller maps `Outcome → TokenId` via `MarketInfo::tokens` when building the `OrderDraft`) and does **not** call `normalize()` — it emits on-grid prices and sizes ≥ the configured minimums so the downstream `normalize` chokepoint only ever bumps, never rejects on size (pinned by the post-only-BUY invariant test). **Float→grid robustness (the most likely silent bug):** ladder prices are computed in f64 (model space, `sigma`/`p_up` are f64), then `price_down` does `Decimal::from_f64_retain(target).round_dp(9)` to absorb f64 subtraction noise (~1e-16, far below the ≤4-dp grid) **before** `Price::quantize(.., Down)` — so a target *meant* to be on-grid (e.g. 0.49 arriving as 0.489999998) is not silently pushed a tick low; both ladders are buys ⇒ both round Down. Verified: all 24 hand-computed expectations matched the real f64 path exactly. **Shared-helper refactor (no formula duplication):** the §8 cap/pair-cost math lived on `WindowInventory` but the calculator gets an `InventorySnapshot` — extracted `excess_constraint_sides`/`pair_cost_after_add_sides`/`authorizes_passive_add_sides` free fns (over raw `&SideInventory`) into `inventory.rs`; `WindowInventory`'s three methods now delegate (behavior-preserving — the existing inventory tests stayed green). All three + the quoting types re-exported from `lib.rs`. **Anti-drift `QuoteParams`-vs-`EngineParams` test NOT added in engine** (engine has no `config` dep by §4 — it would violate inward-arrows); that test lands in `bot` with the deferred config→`QuoteParams` mapping, exactly as `config_maps_into_model_params` covers the others. **Test matrix** (≥14 required, 24 written, each hand-computed): balanced two-sided; min_edge-binds vs vol-term-binds; latency-premium tips the touch a tick lower; inventory-skew direction; soft-cap one-sided; hard-cap `InventoryBlocked`; worst-case-breach silences the excess side; pair-cost rejection vs cheap-add; vol-spike widening (+control); ATM final-seconds per-level suppression; no-passive final-seconds; Unreliable + Degraded gating; **the canonical adverse-selection jump** (p 0.50→0.85, σ spike: Up→0.83/0.82/0.81, Down withdrawn 0.49→0.13/0.12/0.11; asserts not one price survives in place, so a diff withdraws every stale quote); grid-collision dedupe; tick snapping at both grid extremes; ladder size/spacing triples; p_up/σ/τ/window gating; the post-only-BUY-on-grid invariant. §8 fee params are in the signature but unused by passive quoting (makers pay zero — documented; reserved for the taker modules). **Verification:** `cargo fmt --all --check` clean; `cargo clippy -p engine --all-targets -- -D warnings` clean; `cargo test -p engine --lib` **67/67** (24 quoting + the unchanged normalize/inventory suites green, proving the refactor); `cargo check --workspace` clean (bot re-export edge compiles). **4551:** the engine test exe was hash-sticky blocked on the inventory-refactor build (deterministic rebuild reproduced the same blocked hash even after delete); adding the `quoting` module reminted the crate to a fresh, unblocked hash — the documented remedy. **Deferred follow-ups:** the `bot` config→`QuoteParams` boundary map + its anti-drift test; wiring `calculate_quotes → normalize → risk → VenuePort` (the quote manager: cancel-first repricing on θ/cancel_market_theta, which are the manager's, not the calculator's); the taker modules consuming `fees`; an optional live `bot quote` smoke subcommand.

- 2026-06-15 — **Quote manager implemented** (`engine::quote_manager` — new submodule dir: `client_id`/`view`/`plan`/`driver` + `mod`; 21 pure unit tests) + **`crates/bot/tests/quote_manager_paper.rs`** (5 integration tests vs `venue-paper`). The §8 cancel-first convergence engine: owns convergence from the current open orders to the calculator's desired set through the `venue-api` port. **Operator decisions:** (1) **split-cycle (strict) cancel-first** — a stale/endangered quote is cancelled immediately and its replacement placed only on a *later* converge, once the cancel terminalizes and the slot is empty, so old+new never rest on the same side at once (honors the §8 invariant by construction); (2) **engine module + paper integration test only** — full bot orchestration (scheduler+feeds+model+manager on one live bus) and a `bot quote` subcommand stay **deferred** to the engine-bus-wiring task, like every prior engine task's bot wiring. **Architecture:** pure planner/view/client_id (sans-IO, tracing-free) + an async `driver`. **`QuoteManager` is NOT generic** — its methods are generic over `P: VenuePort` (the port is passed per call, never stored), and it needs **no `tokio`**: it only `.await`s the port's `impl Future + Send` methods and takes `now: TimestampMs` as a parameter (sans-clock, computing τ inline). Engine added **only `tracing`** (the driver logs every intent/place/cancel/breaker via target `quote-manager`, §12; the pure modules stay tracing-free — the `feed-clob` precedent); **no `tokio`, no venue-adapter dep** — `engine/tests/architecture.rs` still green. **Engine-side order view (`view::RestingView`):** the manager can't use `venue-live`'s `OrderStore` (§4), so it keeps its own — `by_id: HashMap<OrderId,RestingOrder>` (authority, folds `OrderUpdate`) + `slot: HashMap<(Outcome,u32),OrderId>` (the live id per ladder slot). Simpler than `OrderStore` because the manager placed every order: `record_pending` is fed from the place `Accepted`; lifecycle folds from the venue's order stream; a terminal drops `by_id`+slot. **Strict-overlap rule:** a cancelled order is marked `PendingCancel` and **kept in its slot** (blocking placement there) until its terminal `Canceled` frees it. **`client_id` scheme (`qm:{open_ms}:{U|D}:{level}:{seq}`):** a process-global `seq` makes every placement a distinct id, so a same-slot replace is unambiguous even while old+new briefly coexist in `by_id`; `parse` recovers attribution from the `Accepted`/`PlaceRejection` echo. **Convergence planner (`plan::ConvergencePlanner`, pure):** P0 NoQuote → `cancel_market` (or AlreadyConverged if nothing cancelable); P1 `|Δp_up| > cancel_market_theta` (vs the fair at last quote) + a cancelable view → bulk `cancel_market`, replace next cycle; P2 incremental — **places only into empty slots**, cancels stale (`|p_new−p_old| > θ`, Decimal-space) or unmatched orders, a pending-cancel-occupied slot is `WaitingForCancel`. **One converge never both cancels and places the same slot** (split-cycle). Cancel ids are sorted `(outcome,level,seq)` for determinism (HashMap iteration). **Directionality, layered (operator left the judgment to me, recorded per §2.8):** *Rule A* (planner, symmetric `|Δdesired_price|>θ`) decides keep-vs-replace; *Rule B* (driver fast-path, directional) cancels the **endangered side immediately** on a model tick — Up-buys when `p_up` fell, Down-buys when it rose — **bypassing the rate budget** (a cancel is always safe), escalating to `cancel_market` past `cancel_market_theta`; the replacement waits for the next budget-eligible tick. `p_up_at_last_quote` updates **only on an actual placement** (so Rule B measures drift from the fair the resting orders embed). **Driver fill/inventory source:** the manager owns an `InventoryManager` folded **only** from the venue stream (`on_venue_event(VenueEvent::Fill)` → `Event::Fill`), the authoritative report of *our* executions — `on_event` ignores bus `Fill`/`OrderUpdate` (single-source, no double-count); self-contained for quoting decisions (the alternative, consuming a global `Event::Inventory`, would couple correctness to bus ordering). Empty inventory derives an empty snapshot **for the active window** so the calculator's window-match gate passes. **Rate budget (`QuoteManagerParams`, engine-local Default, config map deferred):** `min_requote_interval_ms` (250) throttles **placements** (cancels never throttled); `reprice_threshold_theta` (0.005) / `cancel_market_theta` (0.02) mirror `config::EngineParams`, pinned by a unit test without a `config` dep; `max_batch` (15) guards the documented batch limit (the port chunks internally). **Dirty model:** triggers (Model/ModelHealth/Fill/book-change-at-our-levels) set `dirty`; `on_requote_tick` clears it **only on `AlreadyConverged`**, so a placement keeps dirty until the next eligible tick confirms convergence (one extra cheap planning pass; never stalls an owed replacement). Book changes coalesce — only a top at/through one of our resting prices dirties (deep-book churn ignored). **Lifecycle/risk:** Window `Open` resets per-window state; `Closing` → `cancel_all` + stand down for the window (the authoritative final-seconds stop, redundant with the calculator's `FinalSecondsNoPassive` τ-gate → `cancel_market`); `Closed`/`Resolved` drop the window (Resolved folds settlement into inventory); `Event::Risk(BreakerTripped|CancelAllIssued)` → `cancel_all` + stand down until `BreakerCleared` empties the tripped set (then re-dirties to reconverge). Non-Ready `ModelHealth` rides every snapshot → calculator `NoQuote` → planner pull. **Integration test (`bot/tests/quote_manager_paper.rs`, `#[tokio::test(start_paused)]`):** manager + real `PaperVenue` share one paused-clock `now` closure; each test feeds bus events to both (venue for fill-sim, manager for quoting), pumps the venue's `VenueEvent` stream back into `on_venue_event`, and drives `on_requote_tick`. Asserts the four required properties: **(a) convergence** — resting orders == a direct `calculate_quotes` ladder (6 levels: Up 0.49/0.48/0.47, Down 0.49/0.48/0.47 at p=0.50), driven off **journal-recorded → replayed** events (`Recorder`→`ReplayReader`, mirroring `inventory_replay.rs`; the session round-trips losslessly — proves "on recorded data"); **(b) cancel-before-replace** — a jump to p=0.85 (|Δ|=0.35) bulk-cancels, and by split-cycle every old `Canceled` strictly precedes every new `Open` (replacements use fresh order ids at the 0.85 ladder); **(c) rate-budget** — 8 rapid supra-θ ticks in one interval → still 1 placement cycle (`place_cycle_count()`), and the budget releases after the interval; **(d) clean final-seconds withdrawal** — Window `Closing` → `cancel_all` → empty, and separately advancing virtual time to τ=4s (≤ `no_passive_final_secs` 5s) → `NoQuote(FinalSecondsNoPassive)` → `cancel_market` → empty. **Verification:** `cargo fmt --all --check` clean; `cargo clippy -p engine --all-targets -- -D warnings` clean; `cargo test -p engine` **87 lib + 2 architecture** green (incl. 21 quote-manager unit tests: client_id round-trip, view slot/terminal/pending-cancel/replace, planner P0/P1/P2 + split-cycle + directionality, params-mirror pin). **`cargo test -p bot` could NOT run in this sandbox** — `venue-live`'s `polymarket_client_sdk_v2` chain currently fails to build (`can't find crate for alloy/async_trait/bon` — a corrupted SDK dependency-cache state, **not** my code: it's a pre-existing infra issue, the same crate built green here 2026-06-14, and nothing I touched goes near `venue-live`/the SDK). The integration test depends only on SDK-free crates (`engine`/`venue-paper`/`venue-api`/`journal`/`core-types`/`tokio`), so I verified it via a **throwaway external harness crate** (`[[test]] path` → the real file, path-deps to those crates only): **5/5 passing**, and `clippy --test … -D warnings` clean; harness then deleted. **Operator: re-run `cargo test -p bot` on the working toolchain** (and regression-check `venue_parity`/`inventory_replay`) once the SDK build state is repaired (a full `cargo clean` + rebuild, or repairing the alloy/aws-lc artifacts), like prior bot-integration runs. **Deferred follow-ups:** the `config::EngineParams → QuoteManagerParams`/`QuoteParams`/`NormalizerParams`/`InventoryParams` boundary maps + anti-drift tests at the `bot` binary; the live orchestration wiring (scheduler→manager on one bus) + an optional `bot quote` smoke; merge-request execution from the manager; a `cancel_batch` on the port if sequential cancel latency ever matters.

- 2026-06-15 — **Momentum taker implemented** (`engine::taker` — new submodule dir: `signal`/`edge`/`driver` + `mod`; 33 new in-crate tests, engine now 128 lib + 2 architecture) + **`crates/bot/tests/momentum_taker_paper.rs`** (2 integration tests vs the real `PaperVenue`). The §8 fee-aware momentum/staleness module: when a fresh move on the fast signal feed implies the Polymarket book has not repriced and the model-vs-book edge (after the exact per-market taker fee + buffer) is still positive, it fires a single **FAK marketable BUY** to pick off the lagging book, sized against displayed depth and the per-window taker budget. Fulfills the momentum half of `lib.rs`'s long-standing "fee-aware momentum and late-window taker modules" promise. **Operator decisions:** (1) scope = engine module + engine unit tests + a paper integration test (matching every prior engine task); config→params map, new config keys, live orchestration, and a `bot momentum` smoke subcommand **deferred** to the engine-bus-wiring task. (2) **NO τ floor** — the taker is purely signal/edge/budget/cooldown/risk driven, takes allowed right up to resolution; only a defensive `τ > 0` expiry guard remains (the `Window` lifecycle already clears the active window on `Closing`). The near-certainty endgame is a separate, future late-window taker. **Architecture (mirrors `quote_manager`):** pure `signal`/`edge` (sans-IO, sans-clock, tracing-free) + an async `driver`. `MomentumTaker` is **not generic** (methods take `port: &P where P: VenuePort` + `now: TimestampMs`); engine added **no deps** (taker uses the existing `model`/`venue-api`/`core-types`/`rust_decimal`/`thiserror`/`tracing`) — `engine/tests/architecture.rs` still green. **Signal detector (`signal::SignalWindow`/`ConfirmedMove`):** per-asset ring of `(ts_local, value)`, pruned to the lookback on push; `confirmed_direction(now, sigma_1s, sigma_mult)` returns `Some` iff ≥2 in-window ticks and `|ln(S_newest/S_oldest)| > sigma_mult·σ_1s·√T`, where `T` = the **actual** span between the oldest- and newest-in-window ticks (self-consistent under bursty arrival), sign → Up/Down. Inputs: **only `BinanceDirect`/`Mid` ticks** feed the ring (the ~2.7 s-leading fast signal, 2026-06-12 compare findings); `σ_1s` comes from the `ModelSnapshot` (so a take is implicitly gated on model `Ready`). **Edge calculator (`edge::plan_take` → `TakePlan`/`NoTakeReason`):** walks `BookSnapshot.asks` ascending, accepting each level while the per-share net edge clears the (rebate-discounted) fee + buffer, caps cumulative notional at the remaining budget (truncating shares mid-level), requires final notional ≥ $1 (pre-empting the normalizer's `BelowMinNotional`), and `worst_price` = highest accepted ask (the FAK slippage cap; **no separate absolute price cap** — the edge gate already precludes uneconomic high-price takes). **Decisions (operator left the judgment calls to me, per §2.8):** (a) **per-share marginal gate, proven monotone** — the per-share fee `rate·p·(1−p)` is non-monotone (peaks at 0.5) and floored, but the per-share *net* edge `(fair−p) − rate·p·(1−p)·(1−reb) − buffer` is **strictly decreasing in p** (derivative `−1 − rate·(1−2p)·(1−reb)`, and `|rate·(1−2p)·(1−reb)| ≤ rate < 1` so the linear edge term dominates) ⇒ "stop at the first failing level" is correct; the **exact `core_types::taker_fee`** (5-dp round, $0.00001 floor) is summed per accepted slice for the reported **gross** `expected_fee` so it matches what the paper/live venue charges (§9: the decision never claims an edge the fill won't realize). (b) **`BookAlreadyRepriced` (best ask ≥ fair, the move was absorbed) vs `EdgeBelowFeePlusBuffer` (positive raw edge, fee+buffer eats it)** are distinct typed reasons — the staleness test made observable for tuning. (c) **fire-once** — a partial FAK leaves a single-sided position to the global inventory/merge machinery; the taker does not chase. **Taker-rebate finding (docs \"Taker Rebates\" read, required by the task):** the program is tiered on 30-day **weighted** volume `wV = size·(1−entry)·weight·bonus` (Bronze $2k→3% … Obsidian $10M→50%), paid **once daily at 00:00 UTC in pUSD, NOT at execution** ⇒ **immaterial at our volume** (tens-to-low-hundreds $/window never reaches Bronze → 0%). Wired as a **default-zero `taker_rebate_pct`** that discounts the *decision* fee (`effective_fee = taker_fee·(1−rebate_pct)` = expected net cost) so the math is present and inert; `expected_fee` stays gross (the rebate is a separate accrual), so journal expected-vs-realized stays apples-to-apples. **Driver (`driver::MomentumTaker`, target `momentum-taker`):** sync `ingest`/`decide`/`on_venue_event`/`on_window`/`on_risk` cores (the whole gate ladder is unit-testable without a runtime) + async `on_event`/`attempt_take`/`fire`. Triggers a take on `Event::Model` and on an active-token `Event::Book` (a fresh fair or fresh depth may open an opportunity); a `PriceTick` only updates the ring (the decision needs a fresh fair) — avoids both thrash and missed takes. **Budget = committed-in-flight aware:** `effective_spent = realized_spent + Σ pending`; `place` `Accepted` commits the planned notional to `pending`, each of our `Fill`s moves notional into `realized` and decrements that order's `pending`, and the order's **terminal** `Order` update drops the residual (a FAK's unfilled remainder was never spent) — so a take can never exceed the budget even if a fill lags the next decision; the cooldown is a behavioral throttle, not a correctness crutch (§9). **Fill attribution by our own `OrderId` set** (`our_orders`), robust when a quoter shares the venue stream (the `Fill` struct has no `client_id`; `debug_assert` liquidity==Taker as a cross-check). Risk veto mirrors the quoter (`tripped`/`standing_down`) but issues **no cancel-all** — a FAK is immediately terminal, so the taker holds no resting orders, it just stops firing. Gate ladder (each → a typed `NoTakeReason` logged at `debug`): standing-down → active window → model present → window match → `Ready` → usable fair/σ → `τ>0` → confirmed move → cooldown → budget → cached book → `plan_take`. FAK built with `client_id \"mt:{open_ms}:{seq}\"`, routed through `normalize` (book `None` — FAK isn't post-only, so the cross check is skipped; it only re-checks the $1 notional and snaps the already-on-grid worst price). **`MomentumTakerParams`** engine-local: `momentum_buffer`/`budget_per_window`/`cooldown_ms` mirror `config::EngineParams` (`taker_momentum_buffer` 0.005, `taker_budget_per_window` $10, `taker_cooldown_ms` 5000), pinned by an anti-drift test without a `config` dep; `signal_lookback_ms` (2000), `signal_sigma_mult` (3.0), `taker_rebate_pct` (0.0) are crate-local (no config key yet) — config exposure + the `bot` boundary map deferred (the `QuoteParams`/`QuoteManagerParams` precedent; **the config crate was not touched**). **Tests:** `signal` (8: confirmed up/down, below-threshold, flat, <2 ticks, unusable σ, pruning/oldest-baseline, dimensional pin), `edge` (12: takes-when-clear, **refuses fee-marginal at p≈0.5** [the steep §8 boundary: fee 0.0175 + buffer 0.005 ⇒ need edge > 0.0225], multi-level depth sizing, stop-at-first-fail, budget cap mid-level, book-already-repriced, below-min-notional, empty asks, **per-market fee-rate flips accept/reject**, rebate discounts decision but reports gross, zero-fee), `driver::decide` + budget reconciliation (13, sync: happy up + mirror down, every gate's reason, cooldown set/release, budget exhausted, fills charge + terminal drops residual, foreign-fill ignored, in-flight blocks a second take, window-open resets, multi-breaker hold). **Integration (`momentum_taker_paper.rs`, `#[tokio::test(start_paused)]`, mirrors `quote_manager_paper.rs`):** (1) a confirmed up-move + a stale-cheap Up book → exactly one FAK that fills 12.5 sh @ 0.80 ($10 budget cap), realized fee == `taker_fee(12.5, 0.07, 0.80)` = $0.14, realized spend == $10 ≤ budget — driven off **journal-recorded → replayed** events (round-trips losslessly); (2) cooldown blocks a second take within the window and releases after 6 s. **Verification:** `cargo fmt --all --check` clean; `cargo clippy -p engine --all-targets -- -D warnings` clean; `cargo test -p engine` **128 lib + 2 architecture** green. **`cargo test -p bot` still blocked** by `venue-live`'s `polymarket_client_sdk_v2` chain failing to build (E0463/E0282 — the same pre-existing SDK dependency-cache breakage logged in the quote-manager entry; **not** this code — nothing here touches `venue-live`). The integration test depends only on SDK-free crates, so I verified it by **temporarily** adding `engine`+`journal` as `venue-paper` dev-deps and running it as `cargo test -p venue-paper --test momentum_taker_paper` (venue-paper has no `venue-live` dep, reuses the warm workspace target/lock — sidesteps both the SDK breakage and the fresh-target 4551): **2/2 passing**, `clippy --test … -D warnings` clean; the temporary dev-deps + copied test were then reverted (the canonical test lives in `crates/bot/tests/`). **Operator: re-run `cargo test -p bot` on the working toolchain** (regression-checking `venue_parity`/`inventory_replay`/`quote_manager_paper`) once the SDK build state is repaired, like prior bot-integration runs. **4551:** the fresh engine `architecture` and `momentum_taker_paper` test exes were hash-sticky blocked; deleting the blocked exe + rebuild cleared each (the documented remedy). **Deferred follow-ups:** the `config::EngineParams → MomentumTakerParams` boundary map + new config keys (`signal_lookback_ms`/`signal_sigma_mult`/`taker_rebate_pct`) + anti-drift test at the `bot` binary; the shared `taker_budget_per_window` reconciliation across the momentum + future late-window taker (config says the $10 is \"both modules combined\" — kept driver-local for now); live orchestrator wiring + an optional `bot momentum` smoke; the late-window taker module.
