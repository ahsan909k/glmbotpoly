[package]
name = "engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
description = "Quoting, inventory/pair accounting, taker modules, risk manager"

[lints]
workspace = true

[dependencies]
model = { path = "../model" }
venue-api = { path = "../venue-api" }
