[package]
name = "core-types"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
description = "Shared domain types, events, ids, and decimal newtypes"

[lints]
workspace = true

[dependencies]
rust_decimal = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
# Round-trip tests only — core-types itself never touches a wire format.
serde_json = { workspace = true }
