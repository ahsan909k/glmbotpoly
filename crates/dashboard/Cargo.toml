[package]
name = "dashboard"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
description = "Axum REST + WebSocket dashboard served by the bot"

[lints]
workspace = true

[dependencies]
analytics = { path = "../analytics" }
engine = { path = "../engine" }
